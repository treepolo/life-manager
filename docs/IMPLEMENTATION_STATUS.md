# 實作狀態與完成證據

> 本檔案是唯一可宣稱功能狀態的地方。Codex必須逐項更新，不得以聊天摘要取代。

## 狀態定義

- `NOT_STARTED`
- `IN_PROGRESS`
- `IMPLEMENTED_UNVERIFIED`
- `AWAITING_USER_SETUP`
- `EXTERNAL_BLOCKED`
- `VERIFIED`

## 2026-08-11 Firstrade 真實樣本線驗收續作

- 本線範圍：`INV-002`、`SETUP-007`、`AT-INV-02`～`AT-INV-05`；工作樹為從乾淨 `master` HEAD `0b370f2` 建立的 `codex/accept-firstrade`／`D:\人生管理器-wt-firstrade`，不包含 Instagram 線未提交變更。
- 開工狀態：`INV-002`／`SETUP-007` 先依正式規則進入 `IN_PROGRESS`；完成本輪可執行核對後，因正式匯入尚未執行，正式狀態維持 `AWAITING_USER_SETUP`。正式匯入前的staging D1 SQL與App完整JSON備份已完成並驗證。已檢查 importer、service、`InvestmentImportPanel`、匯入 API、既有單元／Worker-D1 契約及 `0003_finance_investments.sql`；不需新migration。
- 官方遮蔽樣本證據：被Git忽略的遮蔽副本解析為486列、UTF-8、逗號分隔、0 parse errors，日期範圍為2025-04-03～2026-07-21，檔案SHA-256為`6e0b29d0a3bd9cefa33f28e3e882b141fdd4ce0f53acea26c563c192acfb0e35`。來源`Action`為`BUY`、`SELL`、`Dividend`、`Interest`、`Other`；前四者正規化，`Other`保留為`UNCLASSIFIED`／需人工確認27列。CSV沒有`Currency`欄，USD是mapping profile的default，不冒充來源欄位；`Fee`欄合計0.96但沒有直接`Fee` Action，不臆造費用活動。
- 瀏覽器證據：已在staging `/finance`只上傳遮蔽副本並查看預覽，畫面顯示UTF-8、逗號、總列數486、解析錯誤0，沒有按正式匯入；Firstrade官方帳戶紀錄完整日期範圍顯示總數486，另以Buy／單一symbol／單日唯讀篩選確認第39、40列對應官方畫面中的兩筆相同可見交易紀錄。未讀取或輸出帳號、姓名、地址或未遮蔽內容。
- importer契約修正：官方畫面證明完全相同來源列仍是兩筆交易；`src/integrations/firstrade-csv/importer.ts` 現以同一base dedupe key的決定性 occurrence ordinal產生穩定鍵，保留同檔相同列且重跑仍冪等，不需migration。新增`tests/unit/firstrade.test.ts`固定答案；遮蔽樣本本機D1驗證為首次`totalRows=486`／`importedRows=486`／`duplicateRows=0`／`errorRows=0`，重跑`importedRows=0`／`duplicateRows=486`／`errorRows=0`，活動計數為BUY255、SELL182、DIVIDEND7、INTEREST15、UNCLASSIFIED27，未知27列的原始證據保留。
- 驗證範圍：AT-INV-02～04的既有固定答案加上本輪同檔完全相同來源列測試通過；AT-INV-05已取得官方來源、遮蔽檔預覽列數／錯誤列／活動類型／幣別mapping、官方列數及本機D1金額合計證據，且正式staging D1／App完整JSON備份已完成；正式匯入後App帳本畫面與金額合計核對尚未執行，因此不宣稱`VERIFIED`。
- 自動驗證：`npm run test` 14 files／43 tests、`npm run test:worker` 21/21、`npm run lint`、`npm run typecheck`、`npm run build:client`及`npm run test:e2e`均exit 0；Playwright圖表案例曾因本機Wrangler中斷自動以全新D1重試後通過，沒有修改或放寬產品斷言。`npm run scan`、`npm run verify:requirements`及`git diff --check`亦通過。
- 安全與範圍：臨時真實樣本測試檔已刪除，遮蔽副本仍在被Git忽略的`private-imports`；未遮蔽CSV、帳號、姓名、地址、帳密未進Git、log、snapshot或文件。本輪只修改Firstrade importer、直接單元測試及本線三份文件，未修改Instagram、共用finance、既有migration、DEFER-004或DEFER-005。
- 備份證據（檔案均在被Git忽略的`backups/`，只記錄metadata，不含內容）：D1 SQL `backups/life-manager-staging-pre-firstrade-20260811-214208.sql`，6,015,278 bytes，SHA-256 `26f829c6c1882449ea04a7eb5eaac1836e5f0584d02bb9397f4f48ec77efef72`；App完整JSON `backups/life-manager-staging-full-json-20260811-220617.json`，5,635,104 bytes，檔案SHA-256 `dbcfff0b307a61af561b85f4f6f7bdfd9072eb9261a5bea98d3e7adbfc54181b`，schema 10、58 tables、4,019 rows，內含App checksum `54c5a2bd12f014d67c7e4ad6de027a15ddb9cd2da28ff6119897efe12a2d543a`且重算一致；兩者均有`.sha256` sidecar並確認被`.gitignore`排除。未遮蔽CSV與App JSON內容未進Git、log、snapshot或文件。
- 外部阻擋：正式備份與含修正版本的staging部署已完成；仍需執行正式匯入及App畫面金額合計核對；在取得下一個操作授權前維持`AWAITING_USER_SETUP`。

## 2026-08-12 Firstrade staging部署閘門結果

- 本輪依正式規則先將`INV-002`／`SETUP-007`標為`IN_PROGRESS`。預定處理檔案只包含`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`及`docs/TRACEABILITY_MATRIX.md`的本線狀態／證據行；不修改Firstrade importer、既有migration、共用finance或其他產品線。
- 前置與部署證據：`npm run lint`、`npm run typecheck`及`npm run build`成功；正式`wrangler.toml --env staging --keep-vars`的dry-run成功。staging部署於`2026-08-11T17:13:44.836Z`完成，Worker版本`7d7171c5-7be9-49c8-a28b-e7e0b2dfbe18`承接100%流量，URL為`https://life-manager-staging.life-manager.workers.dev`，未部署production。
- 唯讀smoke證據：未登入`/api/v1/health`回`302`，Access邊界仍在；已登入staging`/finance`載入正式空資料狀態，前端console error為0；遠端D1 migration回報`No migrations to apply`。本輪未套用migration、未執行D1寫入、未讀取Secret值，也未在App按正式匯入。
- 完成後依規則將`INV-002`／`SETUP-007`恢復為`AWAITING_USER_SETUP`。真人CSV阻擋仍具體列為：遮蔽副本在被Git忽略的`private-imports/`，但正式匯入尚未執行；下一個獨立操作才是正式匯入，之後仍需App畫面核對預期筆數／金額合計，才能完成`AT-INV-05`。未遮蔽CSV、帳號、姓名、地址與帳密不得進入Codex、Git、log、snapshot或文件。

## 2026-08-13 Firstrade正式匯入與AT-INV-05完成

- 本輪開始時依正式規則將`INV-002`／`SETUP-007`列為`IN_PROGRESS`；本輪只處理Firstrade真實遮蔽樣本的正式匯入、合計及人工來源核對，未修改其他產品線、既有migration、共用finance、DEFER-004或DEFER-005。
- 正式staging前提：共用staging版本`db41ff0c-7864-43d2-9a98-54000cebfa92`承接100%流量；遠端migration無待套用；正式匯入前D1 SQL與App完整JSON備份、bytes／SHA-256／JSON checksum均已核對；本輪未讀取Secret值。
- 正式staging匯入結果：batch `019ffa7d-2483-7396-87a3-fdd7c8a5ba6e`於`2026-08-13T09:42:02.922Z`完成，`totalRows=486`、`importedRows=486`、`duplicateRows=0`、`errorRows=0`；staging App畫面顯示相同的`486／486／0／0`。
- 去識別D1合計證據：486筆活動均為USD，`SUM(amount_minor)=1781`（USD 17.81）；活動計數為BUY255、SELL182、DIVIDEND7、INTEREST15、UNCLASSIFIED27；`Other` 27列的原始列證據仍保留，未知類型未丟棄；既有重跑契約為0新增／486重複。
- 真人官方來源核對：Firstrade官方「賬戶紀錄」以完整日期範圍顯示總數486；以`04/16/2025`、BUY、單一symbol的唯讀篩選確認兩筆`MSTU`交易，兩筆數量均為`1.00`、金額均為`-5.71`，使用者確認與遮蔽樣本第39、40列一致。未要求貼CSV，未保存或輸出帳號、姓名、地址或未遮蔽內容。
- 受影響驗證：`npm run lint`、`npm run typecheck`、`npm run test`（14 files／43 tests）、`npm run test:worker`（21 tests）、`npm run build:client`、`npm run scan`、`npm run verify:requirements`及完整`npm run test:e2e`均以exit 0通過；另以短路徑重跑正式UI寫入D1案例，desktop、mobile-390及mobile-320均通過。`git diff --check`通過。
- 本輪決定：`AT-INV-02`～`AT-INV-05`證據齊全，`INV-002`／`SETUP-007`由`IN_PROGRESS`轉為`VERIFIED`；本線無外部阻擋。實作仍不包含Firstrade自動provider或深度投資帳務。

## 第一批需求帳本

| 需求ID | 狀態 | 實作路徑 | migration | 測試／證據 | 阻擋／備註 |
|---|---|---|---|---|---|
| CORE-001 | VERIFIED | `src/modules/areas`, `src/app/pages/AreasPage.tsx` | `0001_core.sql` | D1 CRUD/version/audit；Playwright真實寫入 | 無 |
| CORE-002 | VERIFIED | `src/modules/areas`, `src/app/pages/AreasPage.tsx`, `src/worker/api/resources.ts` | `0001_core.sql`，不需新migration | 事業可連收入來源／支出分類／任務／事件／指標／內容／保存檢視；七種D1契約、斷鏈拒絕及UI移除通過 | 無 |
| CORE-003 | VERIFIED | `src/modules/*`, `src/app/pages`, `src/app/hooks/use-resource.ts` | `0001`~`0005`，不需新migration | 領域、事業、任務排程、財務設定、社群手動資料、指標／事件、期限均有新增、修改及封存／恢復或刪除路徑；Worker與Playwright驗收 | 無 |
| CORE-004 | VERIFIED | `src/modules/metrics/formula` | `0001_core.sql` | `formula.test.ts`及D1 20/1000×100=2、除零422 | 無 |
| CORE-005 | VERIFIED | `src/modules/events`, `MetricsPage.tsx` | `0001_core.sql` | 事件CRUD、時間軸overlay、離線寫入路徑 | 無 |
| CORE-006 | VERIFIED | `src/modules/metrics`, `MetricsPage.tsx`, `MetricLineChart.tsx` | `0001_core.sql`，本輪不需新migration | 角色分組、數值／文字序列、CSV與編輯刪除；Recharts座標、鍵盤tooltip、完整provenance及D1資料更新後曲線改變E2E通過 | 無 |
| CORE-007 | VERIFIED | `src/core/provenance`、finance/social/metrics analytics與圖表 | `0001_core.sql`，本輪不需新migration | 27 unit、16 Worker/D1固定答案；圖表展開可見版本、品質、樣本／觀測／缺失／排除、時間窗、篩選、分組、聚合、分母、伺服器計算時間、來源與證據連結 | 無 |
| TASK-001 | VERIFIED | `src/modules/tasks`, `TasksPage.tsx` | `0002_tasks.sql` | recurrence固定答案含每日／週／月／RRULE | 無 |
| TASK-002 | VERIFIED | `src/modules/tasks/service.ts`, 完成歷史UI | `0002_tasks.sql` | 獨立completion、冪等、歷史清單 | 無 |
| TASK-003 | VERIFIED | `src/modules/tasks/service.ts`, `HomePage.tsx` | `0002_tasks.sql`，不需新migration | 2026-08-01~03補產生、非法同日延後400、延後至08-04 version 2；首頁離線完成與同步E2E | 無 |
| TASK-004 | VERIFIED | 任務／領域／事業UI與sync | `0001_core.sql`, `0002_tasks.sql` | 指引欄位、離線完成E2E | 無 |
| FIN-001 | VERIFIED | `src/modules/finance`, `FinancePage.tsx` | `0003_finance_investments.sql` | 多幣帳戶與原幣/TWD分析契約 | 無 |
| FIN-002 | VERIFIED | transaction CRUD/UI | `0003_finance_investments.sql` | 新增、編輯、封存、查詢、來源證據 | 無 |
| FIN-003 | VERIFIED | income sources、analysis、`FinancePage.tsx`圖表 | `0003_finance_investments.sql`，本輪不需新migration | 來源月收入／占比／趨勢與篩選固定答案；圖例、鍵盤tooltip、證據連結及D1更新後點數2→3與曲線路徑改變E2E | 無 |
| FIN-004 | VERIFIED | snapshots、net worth/trend、`src/modules/finance/query.ts` | `0003_finance_investments.sql`，本輪不需新migration | 淨值、配置、趨勢、快照編輯刪除；趨勢由伺服器回傳完整`AnalyticResult`及正式`calculatedAt`，Worker契約通過 | 無 |
| FIN-005 | VERIFIED | `src/core/money`, finance query | `0003_finance_investments.sql` | Decimal換算、匯率證據、缺匯率排除 | 無 |
| FIN-006 | VERIFIED | monthly/category/moving analysis、`src/modules/finance/query.ts` | `0003_finance_investments.sql`，本輪不需新migration | 月總額、分類、3/6/12月移動平均固定答案；三組財務圖表均由API回傳完整provenance並通過Worker schema契約 | 無 |
| FIN-007 | VERIFIED | `src/modules/finance/analytics.ts` | `0003_finance_investments.sql` | 覆蓋率、基準、超過基準月份固定答案 | 無 |
| FIN-008 | VERIFIED | `FinancePage.tsx`, finance query schema、`MetricLineChart.tsx` | 不需新migration | 月／季／年、原幣/TWD、來源／事業／分類／帳戶篩選API及UI；軸、刻度、單位、圖例、tooltip與設定E2E通過 | 無 |
| INV-001 | VERIFIED | investment account/snapshot/UI | `0003_finance_investments.sql` | 手動券商總值與現金納入淨值／配置 | 無 |
| INV-002 | VERIFIED | `src/integrations/firstrade-csv/importer.ts`, `src/integrations/firstrade-csv/service.ts`, `src/app/pages/InvestmentImportPanel.tsx`, `src/worker/api/index.ts` | `0003_finance_investments.sql`；本輪不新增migration | `tests/unit/firstrade.test.ts`、`tests/worker/api-d1.test.ts`固定答案；遮蔽真實樣本486列／0 parse errors／UTF-8逗號；正式staging App結果486／486／0／0；去識別D1 Amount合計USD 17.81（1781 minor units）、活動BUY255／SELL182／DIVIDEND7／INTEREST15／UNCLASSIFIED27、未知Other 27列原始證據保留；重跑0新增／486重複；Firstrade官方紀錄總數486，唯讀篩選確認2025-04-16兩筆MSTU BUY、數量1.00及金額-5.71均一致；正式staging D1 SQL／App完整JSON備份已完成，bytes／SHA-256／JSON checksum均已核對；共用staging版本`db41ff0c-7864-43d2-9a98-54000cebfa92`承接100%，遠端migration無待套用 | AT-INV-02～AT-INV-05及SETUP-007證據完成；不包含自動provider、深度投資帳務，無本線阻擋 |
| INV-003 | VERIFIED | provider policy、scan | 不需 | 無帳密／逆向登入／下單；secret與關鍵字掃描通過 | 無 |
| INV-004 | VERIFIED | investment schema/import boundary | `0003_finance_investments.sql` | 僅保存來源回報活動，不推導成本或損益 | 無 |
| SOC-001 | VERIFIED | social resources、entity-tags | `0004_social_integrations.sql` | content/post分離、平台帳號、內容標籤 | 無 |
| SOC-002 | VERIFIED | social snapshots/UI | `0004_social_integrations.sql` | 多時間點、累積旗標、原始名稱／定義／來源 | 無 |
| SOC-003 | VERIFIED | `src/integrations/structured-csv` | `0004`, `0007` | 預覽／映射／逐列錯誤／原檔／跨批次去重 | 無 |
| SOC-004 | VERIFIED | events + `MetricLineChart` + `SocialPage.tsx` | `0001`, `0004`，本輪不需新migration | Asia/Taipei時間比例、事件類型篩選、hover、鍵盤focus、點擊狀態與對應時間點E2E通過 | 無 |
| SOC-005 | VERIFIED | social analytics/query/UI | `0004_social_integrations.sql`，本輪不需新migration | MEAN/SUM/MEDIAN/DISTRIBUTION比較與保存檢視；平均及五數分布圖表、聚合／分組設定UI驗收通過 | 無 |
| SOC-006 | VERIFIED | `selectFirstDaySnapshot` | `0004_social_integrations.sql` | 15分鐘不足、45分鐘NEAREST固定答案 | 無 |
| SOC-007 | VERIFIED | conversions CRUD/UI | `0004_social_integrations.sql` | 20→40後總成交70、轉化率1.75% D1契約 | 無 |
| SOC-008 | VERIFIED | social provenance、`SocialPage.tsx`, `MetricLineChart.tsx` | `0004_social_integrations.sql`，本輪不需新migration | API provenance及UI計算依據展開、品質／樣本／缺失／分母／來源連結E2E通過 | 無 |
| SOC-009 | VERIFIED | `src/integrations/youtube`, OAuth/sync/UI、`provider-sync.ts`、`provider-raw.ts`、`scheduled/index.ts`、`IntegrationsPage.tsx` | `0004_social_integrations.sql`、`0009_social_snapshot_uniqueness.sql`、已套用staging的`0010_provider_sync_run_payload_links.sql`；本輪不需新migration | scope/PKCE/state/redirect、60分鐘state、有效Analytics query、完整分頁、Pacific日界、token續期、官方撤銷、Cron失敗隔離、重連、Studio 26天精確核對均通過；2026-08-10已登入Codex瀏覽器真實MANUAL按下約1.6秒內顯示停用「同步中」並鎖定撤銷，19秒後成功；D1為fetched 4／updated 32／error 0、order 0～3四類per-run raw各1、545筆linked snapshot、語意重複0、job READY／attempt 0、財務三表0 | 無 |
| SOC-010 | AWAITING_USER_SETUP | `src/integrations/instagram`, OAuth/sync/UI | `0004_social_integrations.sql` | 最小scope/state、adapter、原始payload與密文token完成 | 等待Meta App及真實專業帳號AT-IG-02/03 |
| SOC-011 | VERIFIED | provider registry | 不需 | registry unit；新增provider不改分析核心 | 無 |
| DDL-001 | VERIFIED | deadline CRUD/UI | `0005_deadlines_notifications.sql` | 通用期限、完成歷史、兩級validation | 無 |
| DDL-002 | VERIFIED | schema/templates/UI | `0005_deadlines_notifications.sql` | 範本降級API回400 | 無 |
| DDL-003 | VERIFIED | scheduler/query | `0005_deadlines_notifications.sql` | 單一actionable date與本地發送時間測試 | 無 |
| DDL-004 | VERIFIED | scheduler/deliveries | `0005_deadlines_notifications.sql` | 重複週期、完成停止、dedupe固定答案 | 無 |
| DDL-005 | VERIFIED | 全站/Home警告UI | 不需 | 最高級中斷式、重要級持續顯示 | 無 |
| DDL-006 | VERIFIED | W-8BEN schema/UI | `0005_deadlines_notifications.sql` | 2026-04-18→2029-12-31；確認日與試算並存 | 無 |
| DDL-007 | VERIFIED | tax template/parent child UI | `0005_deadlines_notifications.sql` | 子任務不另啟全站／排程警告 | 無 |
| DDL-008 | AWAITING_USER_SETUP | Web Push encryption/scheduler/device UI | `0005_deadlines_notifications.sql` | 兩裝置密文訂閱與獨立停用D1契約 | 等待真實手機、電腦權限與接收 |
| DDL-009 | AWAITING_USER_SETUP | `src/integrations/resend`, delivery log/test UI | `0005_deadlines_notifications.sql` | adapter、去重、錯誤/重試與secret邊界完成 | 等待Resend key、from及真實收件 |
| OFF-001 | VERIFIED | manifest、`public/sw.js`、App shell、`src/app/providers/PwaUpdate.tsx`、`src/styles.css`、`scripts/stamp-service-worker.mjs`、`scripts/build-client.mjs` | 不需；本輪不修改D1 migration | app shell內容SHA-256版本、network-first／離線fallback、`updateViaCache: none`、outbox安全接管固定答案；320／390／768／1366／1920五viewport均驗證提示固定在初始可視範圍且手機避開同步列。staging outbox 0時安全更新一次，自動reload後正式CSS為`position: fixed`／`z-index: 30`，資料未清除 | 無 |
| OFF-002 | VERIFIED | IndexedDB entities/query cache | 不需 | offline-sync unit、Playwright快取資料 | 無 |
| OFF-003 | VERIFIED | `src/core/sync`, `src/core/network/request-gate.ts`, offline CRUD UI + outbox | `0006_sync.sql`，不需新migration | 27種核心輸入類型離線建立／修改／封存／恢復unit；UI離線修改→重開→封存→重開→恢復→D1 E2E；任務、財務、資產、指標、事件、社群、期限流程E2E | 無 |
| OFF-004 | VERIFIED | sync manager/SW lifecycle | `0006_sync.sql` | 離線建立→outbox 1→恢復→0→D1存在，三尺寸E2E | 無 |
| OFF-005 | VERIFIED | device registration/pull/change log | `0006_sync.sql`，本輪不需新migration | 手機真實離線領域outbox `0→1→0`；不同實體電腦取得同一領域且`0 待同步`；staging D1為areas 1、area APPLIED operation 1／未套用0、area change 1／max cursor 1、有效device 2、兩個cursor皆為1 | 無 |
| OFF-006 | VERIFIED | conflicts API/DataPage | `0006_sync.sql` | LOCAL/SERVER/MERGED，人工合併版本3 | 無 |
| DATA-001 | VERIFIED | full JSON export/import | 不需新migration | checksum、schema、secret排除、空庫還原契約 | 無 |
| DATA-002 | VERIFIED | module CSV export | 不需 | finance/tasks/metrics/social/events/deadlines/import批次，防公式注入 | 無 |
| DATA-003 | VERIFIED | `backup-local.ps1`, `restore-drill.ps1` | 不需 | 2026-08-03備份40,672 bytes、SHA-256驗證、schema 8與108 SQL commands隔離還原成功 | 無 |
| DATA-004 | VERIFIED | CRUD、sync tombstones、import evidence | `0001`~`0006` | true delete→DELETE tombstone；舊離線版本只形成衝突不復活 | 無 |
| UI-001~005 | VERIFIED | `src/styles.css`, `src/app` | 不需／依功能 | 320、390、768、1366、1920px Playwright與全頁截圖；首頁層級、行內超重要期限、對話框、無水平溢位及reduced-motion通過 | 無 |
| UI-006 | VERIFIED | `src/styles.css`, `src/app/providers/PwaUpdate.tsx` | 不需 | 既有資訊層級驗收、五viewport幾何斷言及staging新bundle樣式規則均通過；更新提示固定在初始viewport內，桌面右下、手機底部116px並避開同步狀態與導覽 | 無 |
| UI-CHART-001~010 | VERIFIED | `src/components/charts`, finance/social/metrics pages | 不需 | Recharts wrapper；圖名、雙軸語意、刻度、單位、圖例、鍵盤tooltip、資料品質、完整provenance、事件互動、D1更新曲線E2E逐項通過 | 無 |
| SEC-001~005 | AWAITING_USER_SETUP | Access JWT、AES-GCM、OAuth state/PKCE、export/scan | 視需要 | access/oauth unit+D1、秘密/假資料/RSC掃描通過 | 等待Cloudflare Access真實政策與production檢查 |
| OPS-001, OPS-002, OPS-003, OPS-008 | AWAITING_USER_SETUP | `wrangler.toml`, `package.json`, `docs/OPERATIONS.md`, `docs/SETUP_CHECKLIST.md` | staging D1已由9→10套用新增`0010`；既有migration未修改 | lint、雙typecheck、38 unit、21 Worker/D1、build、12個隔離D1 Playwright與掃描；staging schema 10／raw 14與回填link 14／孤立0／migration list空；Worker `9a72219e-7880-4ec4-b673-0cb99b64791d` 100%、Access未授權302、四個Secret名稱／型別及既有本人政策／兩台實體App證據保留 | staging建置完成；production尚未建立 |
| OPS-004, OPS-005, OPS-006, OPS-007, OPS-009, OPS-010, OPS-011 | AWAITING_USER_SETUP | scheduled retention、provider health、scripts、`docs/OPERATIONS.md` | 不需 | local build/test/13項D1/backup/restore gate通過 | 等待staging／production外部通道、備份及災難流程真實驗收 |

### 跨域第一批ID狀態索引

下列需求原本只以群組列在帳本中；此索引明確列出每個ID，避免範圍表示造成漏追蹤。狀態以最嚴格的共同閘門表示；若群組內含真實雲端驗收，群組維持`AWAITING_USER_SETUP`。

| 類別 | 需求ID | 狀態 | 實作／證據位置 |
|---|---|---|---|
| 產品原則 | PRD-VISION-001, PRD-VISION-002, PRD-VISION-003, PRD-VISION-004, PRD-VISION-005 | VERIFIED | 正式資料流、指引、角色型指標、事業原則、無延後功能空殼；unit/Worker/E2E |
| 非功能 | NFR-001, NFR-002, NFR-003, NFR-004, NFR-005, NFR-006, NFR-007, NFR-008, NFR-009, NFR-010 | AWAITING_USER_SETUP | 本機效能／UI／provenance／export／安全測試通過；等待免費雲端與Access實測 |
| 架構 | ARCH-001, ARCH-002, ARCH-003 | VERIFIED | `src`模組化單體、增量migration、provider registry；需求覆蓋與契約測試 |
| UI | UI-001, UI-002, UI-003, UI-004, UI-005 | VERIFIED | `src/app`, `src/styles.css`；320/390/768/1366/1920px E2E、截圖、首頁層級、reduced-motion與無水平溢位 |
| UI | UI-006 | VERIFIED | `src/styles.css`固定更新提示；320／390／768／1366／1920幾何斷言與staging安全更新後正式CSS核對通過 |
| 圖表 | UI-CHART-001, UI-CHART-002, UI-CHART-003, UI-CHART-004, UI-CHART-005, UI-CHART-006, UI-CHART-007, UI-CHART-008, UI-CHART-009, UI-CHART-010 | VERIFIED | `src/components/charts`；Recharts、軸／刻度／單位／圖例／tooltip／完整provenance／事件互動／D1更新曲線 |
| 安全 | SEC-001, SEC-002, SEC-003, SEC-004, SEC-005 | AWAITING_USER_SETUP | Access/OAuth/密文/匯出邊界與掃描通過；等待production Access政策驗收 |
| 操作 | OPS-001, OPS-002, OPS-003, OPS-008 | AWAITING_USER_SETUP | 完整local verify通過；獨立staging D1／Worker、schema 8、HTTPS資產、API fail-closed、Access未授權／已授權smoke及兩台實體裝置同步完成；等待production階段 |
| 操作 | OPS-004, OPS-005, OPS-006, OPS-007, OPS-009, OPS-010, OPS-011 | AWAITING_USER_SETUP | 保留排程、備份與隔離還原本機通過；等待staging／production外部通道與操作驗收 |
| 外部設定 | SETUP-002 | VERIFIED | 獨立staging D1／Worker、migration 8、部署版本、未授權302、JWK 200、本人health 200、單一Allow政策及電腦／手機實體App全部通過 | 無；跨裝置資料同步另由`OFF-005`追蹤，不混入本設定閘門 |
| 外部設定 | SETUP-003 | VERIFIED | Google Project、OAuth client、必要Worker Secret、真實callback、本人頻道、Studio 26天精確核對、Google App「實際運作中」、撤銷／Cron失敗隔離／重連、refresh token續期均通過；2026-08-10版本14安全更新與新版pending UI、MANUAL成功、四類有序per-run raw、snapshot provenance／唯一性、job與財務隔離全數通過 |
| 外部設定 | SETUP-001, SETUP-004, SETUP-005, SETUP-006, SETUP-008, SETUP-009 | AWAITING_USER_SETUP | `docs/SETUP_CHECKLIST.md`已填名稱、路徑、secret與驗證；待各自真實帳號、裝置或production階段 |
| 外部設定 | SETUP-007 | VERIFIED | `docs/SETUP_CHECKLIST.md`已記錄官方匯出、遮蔽副本位置、不可提交邊界、staging App預覽與Firstrade官方紀錄證據；正式staging D1 SQL／App完整JSON備份已完成並有bytes／SHA-256／JSON checksum證據；共用staging版本`db41ff0c-7864-43d2-9a98-54000cebfa92`已完成正式匯入與唯讀smoke | 遮蔽副本預覽486列／0錯誤；正式App結果486／486／0／0；D1 Amount合計USD 17.81；Firstrade官方兩筆MSTU BUY人工核對一致；未遮蔽原檔與完整JSON不得交給Codex、進Git、進log、snapshot或文件 |

## 2026-08-03 staging雲端接管紀錄

### 2026-08-03 09:44 開工核對

- 本輪需求：`SETUP-002`、`OPS-001`、`OPS-002`、`OPS-003`、`OPS-008`改為`IN_PROGRESS`；已`VERIFIED`的本機功能與migration不得重做、降級或破壞。
- 唯讀基準：`master`、`origin/master`與commit `21a96cb2f5dc46359261b9f0b05a57de56f79bd0`一致，annotated tag `local-verified-baseline`指向同一commit，開工時工作樹乾淨。
- Cloudflare現況：Wrangler 4.118.0已以OAuth登入`bigtreepolo@gmail.com`的帳號；D1清單為空，`life-manager-staging`與`life-manager` Worker均不存在。此結果取代本檔舊有「尚未登入」敘述，但不代表任何雲端需求已完成。
- 程式／文件衝突：`wrangler.toml`只有頂層local D1；`env.staging`尚無獨立`d1_databases`，Wrangler明確警告環境不會繼承頂層binding。這與`SETUP_CHECKLIST.md`要求建立後回填真實ID一致，需在建立資源後修正，不可使用local或production替代。
- 預定修改：`wrangler.toml`、`package.json`、`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`、`docs/OPERATIONS.md`及受影響的`docs/TRACEABILITY_MATRIX.md`證據；預期不需新增或修改migration，只將既有`0001`~`0008`套用到新staging D1。
- 預定測試：建立前完整`npm run verify`；建立後remote migration清單、schema version、使用者資料空表、Worker部署列表、HTTPS靜態殼層、未設定Access時API fail-closed，以及Access完成後的已授權API健康與未授權拒絕smoke。
- 外部阻擋（當時假設，已由下方介面更正取代）：原先依官方英文文件預期從固定選單啟用；真實中文介面改由「網域」頁的生產Worker URL將存取狀態「公開→受限」。其後的單一email policy、team domain與audience仍需逐步核對。Secret不得貼入聊天。Access完成前，真實已授權API smoke與手機／電腦開啟驗收只能維持`AWAITING_USER_SETUP`。
- 本機基準重驗：09:48開始的`npm run verify`於09:57完成，exit 0、510.9秒；lint、雙typecheck、27 unit、16 Worker/D1、client build、12個隔離D1 Playwright、正式碼掃描及112項需求狀態均通過。既有chunk與`rrule` sourcemap警告未變。
- 部署前衝突（09:58）：工作區忽略檔`.wrangler/deploy/config.json`是2026-08-02產生的舊重導，指向已不存在的`dist/life_manager/wrangler.json`，使未指定config的Wrangler在上傳前退出1；遠端Worker未建立。`package.json`的staging／production部署命令改為明確指定版本控管中的`wrangler.toml`，避免暫存重導凌駕正式環境設定；不刪除使用者暫存檔。
- staging D1結果（09:58）：建立`life-manager-staging`，database ID `4a9da6fd-2fe5-41e1-b17f-0f444e60e14c`、APAC；`0001`~`0008`全數remote套用，後續migration list為空。D1查核為schema version 8，`areas`、`task_definitions`、`financial_transactions`、`content_assets`、`deadline_items`均0筆；只存在migration定義的2個社群平台、3個通知通道及2個期限範本。
- staging Worker結果（10:03）：`life-manager-staging`目前100%部署版本`6cf7eaf6-ab7b-4e87-a692-6b74373ad1c0`，URL `https://life-manager-staging.life-manager.workers.dev`，Cron `*/15 * * * *`，binding明確指向上列staging D1，`OAUTH_CALLBACK_BASE_URL`已回填同一origin。Wrangler 4.118.0在Windows完成Cloudflare upload、subdomain與schedule API後仍回傳exit 1；部署列表、DNS/TLS與HTTPS實測均證明版本生效，此異常不冒充正常退出碼並保留於操作證據。
- staging smoke結果（10:04）：首頁200／642 bytes、manifest 200／531 bytes、service worker 200／2817 bytes、`app.js` 200／958,965 bytes、`app.css` 200／14,716 bytes；未設定Access時`GET /api/v1/health`回503及`ACCESS_CONFIGURATION_MISSING`，API fail-closed。Cloudflare Access尚未建立，因此完整已授權health 200、無session由Access邊界拒絕、手機及電腦開啟仍不得宣稱完成。
- Access啟用前的當時狀態：`SETUP-002`與`OPS-001/002/003/008`恢復為`AWAITING_USER_SETUP`，本機功能狀態未降級；當時依官方英文文件描述一鍵啟用，後續已由使用者提供的真實中文「網域／生產URL／公開→受限」流程更正。
- Access介面更正開工（使用者回報）：目前中文介面的真實路徑是進入`life-manager-staging`的「網域」頁面，找到標示「生產」的`life-manager-staging.life-manager.workers.dev`，把其下方存取狀態由「公開」改為「受限」；完成後頁面顯示Access JWT `aud`與JWK URL。先將`SETUP-002`改為`IN_PROGRESS`。預定修改`docs/SETUP_CHECKLIST.md`、`docs/OPERATIONS.md`與本狀態證據，不需migration或程式修改；預定測試為文件殘留字串掃描、112-ID一致性及真實無session Access smoke。外部阻擋仍是尚未取得實際`aud`／JWK team domain與本人登入smoke。
- Access介面更正結果（10:32）：`SETUP_CHECKLIST.md`與`OPERATIONS.md`已改成以生產Worker URL及「公開→受限」狀態為核心，固定選單文字只作已失效歷史記錄。無session請求首頁與`/api/v1/health`均由Cloudflare邊界回302並導向`life-manager.cloudflareaccess.com`；JWK URL `https://life-manager.cloudflareaccess.com/cdn-cgi/access/certs`回200／JSON。實際audience `0cc6ca1ed73a340419af9e457ce44f52c7b2d67e04272440c223ce6610e1feca`與team domain已回填`wrangler.toml` staging vars。`SETUP-002`恢復`AWAITING_USER_SETUP`；下一閘門為本人email Worker Secret、重新部署及已登入health 200，未標`VERIFIED`。
- Access Secret續作開工：使用者回報`ACCESS_ALLOWED_EMAIL`已透過Wrangler互動提示設定；`SETUP-002`改為`IN_PROGRESS`。預定修改為`wrangler.toml`既有Access vars的遠端部署，以及`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`、`docs/OPERATIONS.md`、`docs/TRACEABILITY_MATRIX.md`驗收證據；不需migration。預定測試為只列Secret名稱、Wrangler dry-run／部署、部署列表與binding、無session首頁/API 302、本人session health 200／schema 8、typecheck、正式碼掃描、112-ID一致性。不得讀取或輸出Secret值；若本人session需使用者登入，停在單一登入步驟。
- Access Secret續作結果（11:08）：`wrangler secret list`只回傳`ACCESS_ALLOWED_EMAIL`名稱與`secret_text`型別，未讀取值。dry-run完整解析8個資產、staging D1及Access vars；Wrangler 4.118.0在Windows到達dry-run結束訊息後仍回傳exit 1，沿用部署列表與HTTPS交叉確認方式。遠端上傳、workers.dev與Cron觸發器發布完成，Cloudflare回報目前100%版本`96f1e1b9-0902-4401-afb5-ea2825085e08`；部署後Secret名稱仍存在。無session首頁及`/api/v1/health`均由Access邊界回302，JWK回200／`application/json`。首次完整驗證只因本輪Wrangler dry-run產生且未受版控的`.tmp/access-staging-dry-run` bundle被lint掃入而停止；精確核對為工作區內可再生暫存物後移除。隨後`npm run verify` exit 0、443.5秒，lint、雙typecheck、27 unit、16 Worker/D1、client build、12個隔離D1 Playwright、正式碼／跳過測試掃描及112-ID一致性全部通過。本輪不需migration；`SETUP-002`恢復`AWAITING_USER_SETUP`，下一閘門為本人登入health 200／schema 8，未標`VERIFIED`。
- Access已授權smoke續作開工：使用者回報本人瀏覽器Access session取得`status=ok`、`environment=staging`、`schemaVersion=8`的health回應；不把每次請求用的request ID保存為正式證據。`SETUP-002`改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`、`docs/OPERATIONS.md`與`docs/TRACEABILITY_MATRIX.md`，不需migration或程式修改；預定測試為核對API在路由前執行Access驗證、health直接讀取D1 schema、文件差異、正式碼掃描及112-ID一致性。外部阻擋縮小為Access application的Allow policy是否只包含本人，以及手機／電腦實體裝置驗收。
- Access已授權smoke結果（11:13）：`src/worker/index.ts`確認所有`/api/`路徑先執行`requireAccess`，`src/core/auth/cloudflare-access.ts`會驗證RS256 JWK簽章、期限、issuer、application audience與`ACCESS_ALLOWED_EMAIL`，通過後才由health handler讀取D1 `schema_metadata`。使用者回報的200回應為`status=ok`、`environment=staging`、schema 8，與預期固定答案完全一致；request ID不列入長期證據。本輪不需migration或程式修改。`SETUP-002`恢復`AWAITING_USER_SETUP`，剩餘Access閘門為核對目標hostname application只有Allow／Include／Emails／本人單一值且無其他放行政策，再進行手機與電腦驗收。
- Access policy續作開工：使用者依目標hostname唯讀核對後回覆政策「符合」，代表只有一個啟用中的Allow，唯一放行條件為Include／Emails／本人單一地址，且沒有額外Allow、Bypass、`Everyone`、email domain或只以登入方式放行。`SETUP-002`改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`、`docs/OPERATIONS.md`與`docs/TRACEABILITY_MATRIX.md`，不需migration或程式修改；預定測試為文件差異、正式碼／Secret掃描及112-ID一致性。外部阻擋縮小為手機與電腦兩台實體裝置的App開啟與後續同步驗收。
- Access policy續作結果：使用者回覆「符合」，單一使用者Access policy人工核對通過；不保存本人email或政策畫面中的身分值。本輪不需migration或程式修改。`SETUP-002`恢復`AWAITING_USER_SETUP`，剩餘項目拆成電腦與手機實體App開啟，並銜接`OFF-005`兩裝置同步，不能只用同一台電腦的自動化瀏覽器冒充實體裝置。
- 電腦實體App續作開工：使用者依固定判據回覆「電腦正常」，代表普通電腦瀏覽器已通過Access、顯示品牌「人生管理器」與第一個主要區塊「今日行動中心」，且無紅色載入／API錯誤。`SETUP-002`改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`、`docs/OPERATIONS.md`與`docs/TRACEABILITY_MATRIX.md`，不需migration或程式修改；預定測試為文件差異、正式碼／Secret掃描及112-ID一致性。外部阻擋縮小為手機實體App開啟，之後才可進入兩裝置同步驗收。
- 電腦實體App續作結果：使用者回覆「電腦正常」，固定可見文字與無錯誤判據通過；不保存裝置、瀏覽器或session識別。本輪不需migration或程式修改。`SETUP-002`恢復`AWAITING_USER_SETUP`，剩餘單一步驟為不同的實體手機開啟同一staging App；手機通過後才評估`SETUP-002`狀態並開始`OFF-005`兩裝置真實同步。
- 手機實體App續作開工：使用者依固定判據回覆「手機正常」，代表不同的實體手機瀏覽器已通過Access、顯示品牌「人生管理器」與「今日行動中心」，且無紅色載入／API錯誤。`SETUP-002`改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`、`docs/OPERATIONS.md`與`docs/TRACEABILITY_MATRIX.md`，不需migration或程式修改；預定測試為112-ID狀態一致性、文件差異及正式碼／Secret掃描。若全部通過，`SETUP-002`可改為`VERIFIED`；下一外部閘門為`OFF-005`，必須使用一筆使用者真正要保存的資料完成手機離線新增、上線同步與電腦取得，不建立假同步資料，也不讀取私人內容。
- 手機實體App續作結果：使用者回覆「手機正常」，固定可見文字與無錯誤判據通過；電腦與手機兩台不同類型實體裝置皆已驗收，不保存裝置、瀏覽器或session識別。本輪不需migration或程式修改。staging資源、schema、部署、Access未授權／授權路徑、單一本人政策及兩裝置App開啟的證據齊全，`SETUP-002`改為`VERIFIED`。資料跨裝置流動尚未執行，仍由`OFF-005`維持`AWAITING_USER_SETUP`，不可因兩台都能開啟就提前完成。
- `OFF-005`實體同步開工：使用者已讓手機保持連線並開啟「領域與事業」，回覆「手機領域頁已開」，代表離線前頁面與目前資料已載入。`OFF-005`改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`及`docs/TRACEABILITY_MATRIX.md`驗收證據，不需migration或程式修改；預定驗收依序為手機斷網、離線新增一筆真正要保存的領域、待同步狀態、恢復網路、outbox歸零、電腦取得，以及只讀D1筆數／operation／change log／device cursor核對。不得建立假資料、讀取私人欄位或把內容要求貼入聊天；每次只要求一個實體裝置操作。
- `OFF-005`離線前準備結果：手機在線的「領域與事業」頁已正常載入，第一個實體步驟通過；本輪不需migration或程式修改。等待使用者關閉手機Wi-Fi與行動數據時，`OFF-005`恢復`AWAITING_USER_SETUP`。下一步只驗證手機確實斷網，不建立資料；離線新增與同步留待後續分步進行。
- `OFF-005`手機斷網續作開工：使用者回覆「手機已斷網」，代表「領域與事業」頁保持開啟，Wi-Fi與行動數據已關閉，未重新整理、關頁或新增資料。`OFF-005`改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；先核對斷網前outbox基準應為`0 待同步`，再允許新增一筆真正要保存的領域。外部阻擋仍是後續手機操作與電腦核對；不讀取私人內容。
- `OFF-005`手機斷網結果：手機斷網步驟已記錄，資料尚未變更。本輪不需migration或程式修改。為避免遺留outbox讓新增後筆數失真，`OFF-005`恢復`AWAITING_USER_SETUP`，下一步只讀手機底部同步狀態；若不是`0 待同步`就停止並回報實際數字，不新增資料。
- `OFF-005`outbox基準續作開工：使用者在手機斷網狀態回覆`0 待同步`，確認沒有先前遺留操作。`OFF-005`改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；下一操作是在手機離線建立一筆使用者真正要保存的領域，固定成功答案為資料出現在頁面且同步狀態由0變為`1 待同步`。不讀取、記錄或要求貼出私人欄位。
- `OFF-005`outbox基準結果：手機斷網且`0 待同步`基準通過，本輪不需migration或程式修改。等待使用者離線提交真實領域時，`OFF-005`恢復`AWAITING_USER_SETUP`；若提交後不是`1 待同步`或頁面顯示錯誤，必須停止，不可開網路或重複提交。
- `OFF-005`手機離線新增續作開工：使用者回覆「已建立，1 待同步」，證明手機仍斷網時一筆真正要保存的領域已出現在頁面，outbox由0增加為1；名稱與內容未提供給Codex。`OFF-005`改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`與`docs/TRACEABILITY_MATRIX.md`，不需migration或程式修改；下一固定答案是恢復手機網路後由`online`事件自動同步，待同步數歸零且無錯誤。若自動同步未發生，才另分一步測試手動同步，不可重複建立資料。
- `OFF-005`手機離線新增結果：真實領域離線建立及`1 待同步`固定答案通過；不讀取私人欄位，本輪不需migration或程式修改。等待使用者恢復手機網路時，`OFF-005`恢復`AWAITING_USER_SETUP`。下一步只觀察`online`事件自動同步，不按「立即同步」；若60秒後仍非0，保留outbox並回報，不重新提交。
- `OFF-005`手機自動同步續作開工：使用者回覆「自動同步完成，0 待同步」，證明恢復手機網路後未按手動按鈕，`online`事件在60秒內將單一outbox操作成功清空且無畫面錯誤。`OFF-005`改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`與`docs/TRACEABILITY_MATRIX.md`；不需migration或程式修改。Codex將以Wrangler對staging D1執行唯讀聚合，只查area筆數、area sync operation／change筆數與狀態、有效device／cursor筆數，不選取ID、名稱、內容、user agent或其他私人欄位；之後才要求電腦同步取得。
- `OFF-005`手機自動同步結果：手機端`online`事件自動將outbox `1→0`且無錯誤，固定答案通過。Wrangler 4.118.0目前OAuth仍登入正確帳號且含D1 scope；`d1 info life-manager-staging`成功確認UUID `4a9da6fd-2fe5-41e1-b17f-0f444e60e14c`、APAC、63表與資料庫可達。只含COUNT／狀態／cursor聚合的query在SQL執行前被Cloudflare API以`7403`拒絕；改用正式資料庫名稱的唯一重試未執行，原因是工具用量上限。沒有資料被讀取或修改，不以D1 info冒充SQL結果，也不繞過限制。`OFF-005`恢復`AWAITING_USER_SETUP`；下一步由不同實體電腦同步取得同一資料，該不同IndexedDB端到端結果可直接證明手機資料已經過伺服器往返。
- `OFF-005`電腦取得續作開工（2026-08-09）：使用者回覆「電腦已取得，0 待同步」，證明不同實體電腦的獨立IndexedDB在重新整理／同步後取得手機離線建立的同一領域，且自身outbox為0、無錯誤；私人領域內容未提供給Codex。`OFF-005`改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`、`docs/TRACEABILITY_MATRIX.md`與必要的`docs/OPERATIONS.md`證據，不需migration或程式修改；預定測試為Wrangler唯讀D1聚合、112-ID狀態一致性、正式碼／Secret掃描及文件差異。D1查詢只允許COUNT、狀態與cursor聚合，不讀取ID、user agent或任何私人payload。
- `OFF-005`電腦取得續作結果（2026-08-09）：不同實體電腦已取得手機離線建立的同一筆真實領域，畫面為`0 待同步`且無錯誤。隨後以Wrangler 4.118.0對`life-manager-staging`執行唯讀聚合，成功取得`areas=1`、area `APPLIED` operation=1、非`APPLIED`=0、area change=1、max area cursor=1、有效device=2、cursor=2、min/max pulled cursor=1；Cloudflare回報APAC／HKG primary、`rows_written=0`、`changes=0`。查詢未選取任何ID、名稱、內容、user agent或私人payload；2026-08-03的API `7403`限制已由本次成功重試收斂。本輪不需migration或程式修改，兩台實體裝置端到端與D1聚合證據完整，`OFF-005`改為`VERIFIED`。
- YouTube真實授權階段開工（2026-08-09）：`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`，先依Google目前官方文件核對Cloud project、Google Auth Platform、Web application client與私人單一使用者的驗證策略。預定修改`docs/SETUP_CHECKLIST.md`、`docs/REFERENCES.md`與本狀態證據；不需migration或程式修改。預定驗收為建立獨立project、啟用YouTube Data API v3與YouTube Analytics API、建立正確redirect URI的Web client、以Cloudflare Secret安全設定client憑證、本人真實頻道授權、首次同步及原始／正規化D1證據。外部阻擋為使用者須登入管理本人YouTube頻道的Google帳號；一次只提供一個平台操作，secret不得貼入聊天。
- YouTube設定文件核對結果（2026-08-09）：官方文件確認project是API與資源的隔離單位、server-side web app需Web application OAuth client且redirect URI必須精確匹配；個人自用或少數已知使用者可不提交公開驗證。Google Auth Platform若維持`Testing`，YouTube等非基本身分scope的授權與refresh token會在7日後到期，因此只適合初次測試，不當作長期自用完成方案。`SETUP_CHECKLIST.md`已填入真實staging callback並移除placeholder，正式production callback仍等待`SETUP-009`真實hostname。因下一步必須由使用者建立／選擇Google Cloud project，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。
- 完整閘門重跑衝突（2026-08-09）：本輪文件收斂後的`npm run verify`中，lint、雙typecheck、27/27 unit、16/16 Worker／D1與client build已通過；E2E第一個全新D1套用`0001`~`0008`後，Wrangler在本機Worker就緒前以Windows exit `3221226505`結束。產品斷言尚未開始、遠端D1未受影響。`scripts/run-e2e.mjs`原有重試只包住Playwright執行後的崩潰，`waitUntilReady`直接拋錯會略過已記載的「Worker死亡才以全新D1重試」規則，屬測試執行器與正式操作文件衝突。此測試閘門修正改為`IN_PROGRESS`；預定只修改`scripts/run-e2e.mjs`與本狀態證據，不需migration或runtime變更，並重跑完整`npm run verify`。斷言失敗仍不得重試，只有已確認的Wrangler程序退出可重試最多兩次。
- 完整閘門診斷續作（2026-08-09）：執行器修正後已實際證明就緒前崩潰會以全新D1重試兩次並在用盡後維持exit 1；受限與核准的非沙箱執行結果相同，4173連接埠、殘留Node／workerd程序與Windows Application事件均無衝突，Wrangler debug log停在列出binding後，獨立`workerd.exe --version`可正常回報。固定版本為Wrangler 4.118.0／Miniflare `5.20260730.0-alpha`／workerd `1.20260730.1`；官方2026-08-07最新Wrangler 4.120.0已更新至Miniflare `5.20260801.1-alpha`與workerd `1.20260801.1`。在不變更runtime程式、migration或遠端資源的前提下，預定另修改`package.json`、`package-lock.json`做單一Wrangler patch更新並重跑完整閘門；若新版仍失敗即記為外部工具阻擋，不再用簡化伺服器替代E2E。
- 完整閘門診斷結果（2026-08-09）：Wrangler 4.120.0／Miniflare `5.20260801.1-alpha`／workerd `1.20260801.1`仍在相同位置中斷，執行器每次都以全新D1重試兩次後維持exit 1；因此撤回無效升級，`package.json`／`package-lock.json`恢復原鎖定4.118.0。保留`scripts/run-e2e.mjs`的就緒前崩潰重試修正，因該分支已被4.118.0與4.120.0實際觸發並證明不會重用D1或忽略非零結果。兩次完整`npm run verify`及獨立E2E均在Playwright斷言開始前受本機Wrangler／workerd啟動阻擋；lint、雙typecheck、27/27 unit、16/16 Worker／D1、client build已通過，正式碼／Secret掃描與112-ID閘門另獨立通過。遠端staging、既有2026-08-03完整12/12 E2E證據及所有runtime/migration均未改動；本輪不得宣稱新的完整E2E通過，也不以Vite或mock server替代。此測試執行器項目結束`IN_PROGRESS`並記錄為外部工具阻擋，需求帳本狀態不因此改寫。
- YouTube Google Cloud project續作開工（2026-08-09）：使用者回覆「Google Cloud 專案已建立」，代表已在管理本人YouTube頻道的Google帳號下建立專用project；project name為`life-manager-personal`，後續由使用者更正實際Project ID為`life-manager-personal-505006`。不要求或記錄Google登入資訊、project number或其他私人帳號資料。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；預定檢查為112-ID狀態一致性、正式碼／Secret掃描與文件差異。外部阻擋縮小為依官方API Library逐一啟用`YouTube Data API v3`及`YouTube Analytics API`；必須一次只給一個平台操作。
- YouTube Google Cloud project續作結果（2026-08-09）：使用者建立Project ID `life-manager-personal-505006`的真實平台步驟已記錄完成；原本把兩個API合併成一個checkbox的設定文件已拆成`YouTube Data API v3`與`YouTube Analytics API`兩個獨立成功判據，符合一次一個操作。本輪不需migration或程式修改；等待第一個API由使用者在Google API Library啟用時，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。唯一下一步是啟用`YouTube Data API v3`，不建立API key、OAuth client或secret。
- YouTube Data API續作開工（2026-08-09）：使用者回覆「YouTube Data API v3 已啟用」，證明Project ID `life-manager-personal-505006`已在Google API Library開啟程式用來讀取本人頻道與影片資源的正式服務；沒有建立API key、OAuth client或secret。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；預定檢查為112-ID狀態一致性、正式碼／Secret掃描及文件差異。外部阻擋縮小為啟用獨立的`YouTube Analytics API`，仍一次只提供一個平台操作。
- YouTube Data API續作結果（2026-08-09）：`YouTube Data API v3`真實啟用證據已寫入設定清單，本輪不需migration或程式修改。此步只證明Google project具備Data API服務，不代表OAuth client、本人頻道授權或同步已完成；等待使用者啟用`YouTube Analytics API`時，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。
- YouTube Analytics API續作開工（2026-08-09）：使用者回覆「YouTube Analytics API 已啟用」，證明Project ID `life-manager-personal-505006`的第二個必要Google服務已在API Library開啟；兩個API皆完成，但OAuth client、本人頻道授權與同步尚未開始。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`及必要的`docs/REFERENCES.md`，不需migration或程式修改；先依Google目前官方Google Auth Platform介面核對Branding／Audience／Clients的順序、私人單一使用者與7日Testing token限制，再提供一個不含secret的平台操作。預定檢查為112-ID狀態一致性、正式碼／Secret掃描及文件差異。
- YouTube Analytics API續作結果（2026-08-09）：`YouTube Analytics API`真實啟用證據已寫入設定清單，Project ID `life-manager-personal-505006`現已具備Data與Analytics兩項必要服務。依Google目前官方介面，OAuth初始設定由Google Auth Platform Overview的`Get Started`進入，後續依App Information、Audience與Contact Information分步；設定清單已拆成一次一個操作。私人策略仍是先以`Testing`完成真實驗收，因非基本身分scope的Testing refresh token七日到期，長期自用後續改`In production`並接受未驗證App警告／使用者上限，不建立公開onboarding或申請公眾驗證。本輪不需migration或程式修改；等待使用者只開啟`Get Started`第一頁時，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。
- Google Auth Platform App Information續作開工（2026-08-09）：使用者回覆「App Information 已開啟」，證明目前已在Project ID `life-manager-personal-505006`的Google Auth Platform初始設定第一頁，尚未填入或提交欄位。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；下一個單一步驟只填App name `人生管理器`，並由Google下拉選取使用者本人會查看的User support email後按Next。email不得貼入聊天；預定檢查為112-ID狀態一致性、正式碼／Secret掃描及文件差異。
- Google Auth Platform App Information開啟結果（2026-08-09）：`Get Started`入口與第一頁已由使用者實際確認，設定清單標記完成；本輪不需migration或程式修改。因App Information尚未提交，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。唯一下一步只完成此頁的App name與User support email，不同時設定Audience、contact、scope或client。
- Google Auth Platform App Information提交續作開工（2026-08-09）：使用者回覆「App Information 已完成」，證明App name已設為`人生管理器`、User support email已由本人在Google下拉選取並進入下一頁；email值未提供給Codex。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；下一個單一步驟只在Audience選`External`並前往下一頁。此選擇僅是Google OAuth user type，不建立多租戶或公開onboarding；預定檢查為112-ID狀態一致性、正式碼／Secret掃描及文件差異。
- Google Auth Platform App Information提交結果（2026-08-09）：App name與私人支援email的真實設定步驟已寫入清單，本輪不需migration或程式修改。Audience尚未提交，因此`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。唯一下一步只選`External`並按Next，不同時填contact、加入test user或建立client。
- Google Auth Platform Audience續作開工（2026-08-09）：使用者回覆「Audience 已完成」，證明Project ID `life-manager-personal-505006`的OAuth user type已選`External`並進入下一頁；App尚未發布，未建立公開onboarding或其他使用者連線流程。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；下一個單一步驟只在Contact Information填使用者本人會查看的developer contact email並按Next，email不得貼入聊天。預定檢查為112-ID狀態一致性、正式碼／Secret掃描及文件差異。
- Google Auth Platform Audience續作結果（2026-08-09）：`External` user type的真實設定步驟已寫入清單，本輪不需migration或程式修改。Contact Information尚未提交，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。原本把contact、policy同意與完成初始設定合併的checkbox已拆開；唯一下一步只填developer contact email並按Next，不同時同意policy、加入test user或建立client。
- Google Auth Platform Contact Information續作開工（2026-08-09）：使用者回覆「Contact Information 已完成」，證明developer contact email已由本人在Google平台填入並進入Finish頁；email值未提供給Codex。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；下一個單一步驟只核對Finish摘要、勾選同意Google API Services User Data Policy並按頁面主要完成按鈕。成功判據是回到Google Auth Platform Overview且初始設定完成；不建立OAuth client、secret或發布App。預定檢查為112-ID狀態一致性、正式碼／Secret掃描及文件差異。
- Google Auth Platform Contact Information續作結果（2026-08-09）：developer contact email的真實設定步驟已寫入清單，本輪不需migration或程式修改。Finish尚未提交，因此`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。唯一下一步只完成Finish摘要與policy同意，成功後停在Overview；不加入test user、不發布App、不建立OAuth client或secret。
- Google Auth Platform初始設定續作開工（2026-08-09）：使用者回覆「Google Auth Platform 初始設定完成」，證明Project ID `life-manager-personal-505006`已完成App Information、`External` Audience、developer contact與Google API Services User Data Policy確認並回到Overview；App尚未發布，OAuth client與secret尚未建立。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；下一個單一步驟只在Audience的Test users加入管理本人YouTube頻道的同一Google帳號並儲存，email不得貼入聊天。預定檢查為112-ID狀態一致性、正式碼／Secret掃描及文件差異。
- Google Auth Platform初始設定續作結果（2026-08-09）：Finish與policy確認的真實平台步驟已寫入清單，本輪不需migration或程式修改。本人test user尚未加入，因此`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。唯一下一步只在Audience加入一個本人Google帳號並儲存；不提供email給Codex，不同時發布App、設定scope或建立client。
- Google Auth Platform test user續作開工（2026-08-09）：使用者回覆「本人測試使用者已加入」，證明Audience的Test users已加入管理本人YouTube頻道的同一Google帳號；email值未提供給Codex，App仍未發布。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；下一個單一步驟只在Data Access加入程式實際要求的`youtube.readonly`與`yt-analytics.readonly`兩個scope並儲存，不加入收益或寫入scope。預定檢查為112-ID狀態一致性、正式碼／Secret掃描、文件差異，以及核對程式scope常數與設定文件一致。
- Google Auth Platform test user續作結果（2026-08-09）：本人test user的真實平台步驟已寫入清單；`src/integrations/youtube/provider.ts`與設定文件一致，只宣告`youtube.readonly`、`yt-analytics.readonly`，未宣告monetary、upload、force-ssl或其他寫入scope。本輪不需migration或程式修改。Data Access尚未儲存，因此`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。唯一下一步是在一個Data Access scope選擇操作中只加入上述兩個唯讀scope；不發布App或建立OAuth client。
- Google Auth Platform Data Access續作開工（2026-08-09）：使用者回覆「兩個唯讀 scope 已儲存」，證明Project ID `life-manager-personal-505006`的Data Access只加入`youtube.readonly`與`yt-analytics.readonly`；依使用者操作指示未加入monetary、upload、force-ssl或其他寫入scope，App仍未發布。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；下一個單一步驟建立`Web application` OAuth client `life-manager-staging`，唯一Authorized redirect URI為`https://life-manager-staging.life-manager.workers.dev/oauth/youtube/callback`。建立後不得把client ID或secret貼入聊天，先保留Google憑證頁；預定檢查為callback路由／provider常數、112-ID狀態一致性、正式碼／Secret掃描及文件差異。
- Google Auth Platform Data Access續作結果（2026-08-09）：兩個真實唯讀scope的儲存步驟已寫入設定清單；`src/integrations/youtube/provider.ts`仍只宣告`youtube.readonly`與`yt-analytics.readonly`，callback契約測試仍覆蓋`/oauth/youtube/callback`，未增加收益或寫入權限。本輪不需migration或程式修改。OAuth client尚未建立，因此`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`；唯一下一步是在Clients一次完成Web application client與唯一staging redirect URI，不填JavaScript origin，憑證值不進聊天。
- YouTube OAuth client與Project ID更正開工（2026-08-09）：使用者回覆`Web application` OAuth client `life-manager-staging`已建立，並更正Google Cloud project name是`life-manager-personal`、實際Project ID是`life-manager-personal-505006`；先前把project name放入Console深連結會進入缺權限頁，屬文件識別錯誤。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，並掃描所有正式Markdown中的Google Console URL／專案識別；不需migration或程式修改。預定檢查為唯一staging callback契約、112-ID狀態一致性、正式碼／Secret掃描及文件差異。OAuth client ID與secret不得讀取、寫入文件或貼入聊天；後續只透過Wrangler互動提示逐一設定Cloudflare Secret。
- YouTube OAuth client與Project ID更正結果（2026-08-09）：Project ID `life-manager-personal-505006`下的`Web application` OAuth client `life-manager-staging`已由使用者建立，唯一staging callback與正式設定清單一致；client ID／secret值未提供給Codex、未寫入Git或文件。`SETUP_CHECKLIST.md`現明確區分project name `life-manager-personal`與Project ID `life-manager-personal-505006`，Google Console Clients深連結已改用真實Project ID，所有正式Markdown已掃描，不再以project name作`?project=`值。本輪不需migration或程式修改。OAuth provider 2/2 unit、Worker/D1與callback契約16/16、112-ID一致性、正式碼／Secret掃描及`git diff --check`皆通過；`rrule`僅有既知缺少sourcemap來源警告。Cloudflare尚未設定兩個Google憑證Secret，因此`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`；下一步先只透過Wrangler互動提示設定`GOOGLE_CLIENT_ID`。
- YouTube Client ID Secret續作開工（2026-08-09）：使用者回覆`GOOGLE_CLIENT_ID`已透過Wrangler互動提示設定；Secret值未貼入聊天。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；以Wrangler 4.118.0對staging執行唯讀`secret list`，只核對名稱與型別，不讀取或輸出值，並執行112-ID一致性、正式碼／Secret掃描及文件差異。外部阻擋縮小為另一次安全設定`GOOGLE_CLIENT_SECRET`，一次只處理一個Secret。
- YouTube Client ID Secret續作結果（2026-08-09）：Wrangler 4.118.0沙箱內第一次唯讀列舉因無法寫入使用者AppData log並在API前`fetch failed`而停止；經核准的同一個唯讀命令重試exit 0，staging只列出既有`ACCESS_ALLOWED_EMAIL`與新`GOOGLE_CLIENT_ID`，兩者型別均為`secret_text`。未查詢、讀取或輸出任何Secret值，本輪不需migration、程式修改或部署。`GOOGLE_CLIENT_SECRET`尚未設定，因此`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`；下一步只透過另一次Wrangler互動提示設定client secret。
- YouTube Client Secret續作開工（2026-08-09）：使用者回覆`GOOGLE_CLIENT_SECRET`已透過Wrangler互動提示設定；Secret值未貼入聊天。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；以目前鎖定的Wrangler對staging執行唯讀`secret list`，只核對Google兩個Secret的名稱與型別，不讀取或輸出值，並核對正式程式在OAuth callback前是否要求`TOKEN_ENCRYPTION_KEY`。預定測試為112-ID一致性、正式碼／credential pattern掃描及文件差異；外部阻擋依唯讀結果決定，未驗證前不部署或開始真實授權。
- YouTube Client Secret續作結果（2026-08-09）：Wrangler 4.118.0遠端唯讀清單exit 0，staging現有`ACCESS_ALLOWED_EMAIL`、`GOOGLE_CLIENT_ID`、`GOOGLE_CLIENT_SECRET`三個名稱且型別皆為`secret_text`；未讀取或輸出任何值。正式程式`src/core/crypto/secrets.ts`要求`TOKEN_ENCRYPTION_KEY`必須是剛好32位元組的Base64 AES-GCM金鑰，`src/worker/api/oauth.ts`會在建立PKCE state時立即用它加密verifier與authorize URL；目前遠端清單沒有此名稱，若直接授權會得到`OAUTH_CONFIGURATION_MISSING`而不是可用整合。本機固定格式檢查確認文件中的產生器輸出可還原為剛好32位元組且未列印值；112-ID一致性、正式碼／credential pattern掃描與`git diff --check`皆通過。本輪不需migration、程式修改或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。下一步只用本機密碼學亂數直接經stdin建立`TOKEN_ENCRYPTION_KEY`，不顯示或人工傳遞金鑰值。
- YouTube token加密金鑰續作開工（2026-08-09）：使用者回覆`TOKEN_ENCRYPTION_KEY`已由本機32位元組密碼學亂數經stdin設定；金鑰值未顯示或貼入聊天。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或程式修改；以Wrangler唯讀列舉staging Secret名稱／型別、版本與部署狀態，確認四個必要Secret存在且不讀取值，並掃描工作樹的Google credential／金鑰值模式。若Secret更新已產生活躍部署，只記錄真實版本；若尚未活躍，才依現有正式staging部署流程處理，不以猜測冒充部署完成。
- YouTube token加密金鑰續作結果（2026-08-09）：Wrangler 4.118.0遠端唯讀清單exit 0，staging的`ACCESS_ALLOWED_EMAIL`、`GOOGLE_CLIENT_ID`、`GOOGLE_CLIENT_SECRET`、`TOKEN_ENCRYPTION_KEY`四個必要名稱皆存在且型別為`secret_text`；未讀取或輸出任何值。Secret更新建立Worker版本7 `bfa4fe4d-d379-404c-a21a-5e40f7df114f`，`deployments status`確認它已取得100%流量且觸發來源為`secret`，因此不再重複執行deploy；更新後無session health仍回302，Access未被繞過。不保存版本紀錄中的帳號識別。OAuth最小scope／PKCE unit 2/2、112-ID一致性、正式碼／credential與32位元組Base64值掃描、`git diff --check`皆通過。本輪不需migration或程式修改；真實Google授權尚未開始，因此`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。下一步先由使用者只開啟已登入Access的`/integrations`頁確認真實YouTube區塊，不在同一步按授權。
- YouTube真實授權入口續作開工（2026-08-09）：使用者回覆「YouTube 授權入口正常」，證明已登入Cloudflare Access的真實staging `/integrations`頁可載入，YouTube區塊同時顯示「尚未授權頻道」與「開始正式授權」，且無紅色錯誤；本步未按授權按鈕。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration、程式修改或部署；預定檢查為OAuth authorize API／UI路徑、112-ID一致性、正式碼／credential掃描及文件差異。外部阻擋縮小為真實Google導向、本人帳號授權、callback及首次同步；仍一次只執行一個平台操作。
- YouTube真實授權入口續作結果（2026-08-09）：真實staging YouTube未授權畫面與無錯誤判據已寫入設定清單。`IntegrationsPage.tsx`的按鈕文字為「開始正式授權」，只會以新operation ID呼叫`POST /api/v1/integrations/youtube/authorize`；伺服器會產生PKCE verifier／challenge與state、以`TOKEN_ENCRYPTION_KEY`加密保存，回傳Google authorize URL後才由瀏覽器導向。Worker/D1與OAuth callback契約16/16、112-ID一致性、正式碼／credential掃描及`git diff --check`皆通過；`rrule`只有既知缺少sourcemap來源警告。此步尚未建立真實OAuth state或授權連線，本輪不需migration、程式修改或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。下一步只按一次YouTube的「開始正式授權」，停在Google頁面確認App名稱，不選帳號、不同意權限。
- YouTube Google導向續作開工（2026-08-09）：使用者回覆「已到 Google 授權頁，App 名稱正確」，證明真實staging authorize API已成功建立加密PKCE／state並導向Google，Google頁面辨識OAuth client的App名稱為`人生管理器`；使用者依指示尚未選帳號、按繼續或同意權限，也未提供含OAuth參數的網址。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration、程式修改或部署；預定檢查為authorize參數契約、112-ID一致性、正式碼／credential掃描與文件差異。外部阻擋縮小為確認本人test user、Google警告／權限頁、callback及首次同步，仍一次只做一個平台操作。
- YouTube Google導向續作結果（2026-08-09）：真實Google頁面與正確App名稱已寫入設定清單。`YouTubeProvider.authorize`固定要求兩個唯讀scope、`response_type=code`、`access_type=offline`、`include_granted_scopes=true`、`prompt=consent`、隨機state與S256 PKCE；未要求monetary、upload、force-ssl或其他寫入scope。OAuth最小scope／PKCE unit 2/2、112-ID一致性、正式碼／credential掃描及`git diff --check`皆通過。使用者尚未選定test user或提交Google同意，本輪不需migration、程式修改或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。下一步只確認或選定管理本人頻道且已列入Test users的Google帳號，email不進聊天，停在繼續／警告／權限頁而不提交。
- YouTube本人test user續作開工（2026-08-09）：使用者回覆「本人測試帳號已選定」，證明目前Google OAuth流程已確認或選擇管理本人YouTube頻道、且先前加入Test users的同一Google帳號；email未提供給Codex，使用者依指示尚未按繼續或同意權限。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration、程式修改或部署；預定檢查為112-ID一致性、正式碼／credential掃描及文件差異。Google下一畫面可能是未驗證App警告或權限同意頁，必須先由使用者只讀辨認實際主標題，不猜測或跨步提交。
- YouTube本人test user續作結果（2026-08-09）：本人頻道管理帳號與Google Auth Platform Test users身分已在真實OAuth流程中選定，email未提供給Codex；使用者尚未按繼續或同意權限，因此沒有真實callback、token或連線紀錄。112-ID一致性、正式碼／credential掃描及`git diff --check`皆通過。本輪不需migration、程式修改或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。Google對Testing App與敏感唯讀scope可能呈現的下一畫面必須以使用者實際頁面為準；下一步只讀回報去敏主標題，不按任何按鈕、不貼email或OAuth網址。
- YouTube未驗證App警告續作開工（2026-08-09）：使用者只讀回報目前Google頁面主標題為「這個應用程式未經 Google 驗證」，未按任何按鈕，也未提供email或OAuth網址。這與私人單一使用者、`External`／`Testing`、未提交公開驗證的既定策略一致，但仍不代表Google權限已授予。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，依Google現行官方個人使用／驗證說明核對警告流程，不需migration、程式修改或部署；在沒有實際畫面證據前不應假設舊版按鈕，必須先核對可見動作再前進。
- YouTube未驗證App警告續作結果（2026-08-09）：真實警告標題已寫入設定清單。Google官方個人使用／development／staging說明允許不提交公開驗證，但本人授權仍可能看到未驗證App警告；目前畫面與既定私人單一使用者策略一致，不建立公開OAuth onboarding，也不把警告當成授權成功。正式參考仍由`docs/REFERENCES.md`的Google官方brand verification文件支撐；112-ID一致性、正式碼／credential掃描及`git diff --check`皆通過。本輪不需migration、程式修改或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。其後由使用者截圖證實現行介面只有「繼續」與「返回安全的位置」，本段不再保留舊版動作指示。
- YouTube未驗證App現行介面更正開工（2026-08-09）：使用者指出實際Google警告頁沒有舊版「進階」，只顯示「繼續」與「返回安全的位置」，且尚未按任何按鈕；使用者提供的截圖已由Codex目視核對。畫面同時明示App處於測試階段，只有信任邀請開發人員時才繼續，符合本人同時為開發者、頻道管理者與test user的私人情境。先前「進階」指示屬過時介面假設，`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，移除依賴舊按鈕的步驟，改以警告內容、可見動作與成功判據描述；不需migration、程式修改或部署。下一個操作只能按一次「繼續」，不得同時提交後續scope。
- YouTube未驗證App現行介面更正結果（2026-08-09）：目視證據確認頁面標題為「這個應用程式未經 Google 驗證」，內文說明App正處於測試階段，且僅有「繼續」與「返回安全的位置」兩個動作；沒有「進階」。設定清單與狀態歷史已改為以實際警告內容、可見動作及成功判據為準，不再把容易過時的舊按鈕當作前置步驟；截圖仍留在使用者系統暫存位置，未複製進專案或Git。112-ID一致性、正式碼／credential掃描及`git diff --check`皆通過。本輪不需migration、程式修改或部署；使用者尚未按任何按鈕，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。下一步只按一次「繼續」並停在下一頁，不在同一步同意scope。
- YouTube Testing警告繼續續作開工（2026-08-09）：使用者依現行介面只按一次「繼續」後回報下一頁主標題為「`life-manager.workers.dev`要求存取您的 Google 帳戶」；未回報email／OAuth網址，也未按後續按鈕或同意scope。這證明流程已從Testing警告進入真正Google權限頁，但仍不代表授權成功。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration、程式修改或部署；下一步只讀核對Google實際呈現的權限說明，不勾選、不繼續，避免以程式宣告scope冒充真實同意畫面。
- YouTube Testing警告繼續續作結果（2026-08-09）：現行警告頁的「繼續」動作已通過，真實Google權限頁以`life-manager.workers.dev`為主標題識別並等待本人決定；使用者尚未提交任何scope，因此沒有callback、token或連線紀錄。程式仍只宣告`youtube.readonly`與`yt-analytics.readonly`，但真實驗收必須再由畫面逐項確認實際權限說明。112-ID一致性、正式碼／credential掃描及`git diff --check`皆通過。本輪不需migration、程式修改或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。下一步保持頁面不動，只讀回報每一項去敏權限說明，不勾選、不按按鈕。
- YouTube真實scope畫面續作開工（2026-08-09）：使用者只讀回報Google權限頁列出「查看您的 YouTube 帳戶」與「查看依據您的 YouTube 內容產生的 YouTube 數據分析報表」兩項存取權；依指示未勾選或按任何按鈕，未提供email或OAuth網址。兩項分別對應程式唯一宣告的`youtube.readonly`與`yt-analytics.readonly`，畫面沒有收益、上傳、編輯、刪除、管理或其他寫入權限。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration、程式修改或部署；預定檢查為scope常數／unit、112-ID一致性、正式碼／credential掃描及文件差異。下一步只讀辨認兩項目前是否已有勾選框／勾選狀態與頁面主要按鈕文字，不點擊，避免沿用過時UI假設。
- YouTube真實scope畫面續作結果（2026-08-09）：Google實際呈現的兩項權限說明與`YouTubeProvider`唯一兩個scope逐項一致，且人工確認未出現收益或任何寫入能力；這只完成同意前的最小權限驗收，尚未產生callback、token或連線。OAuth最小scope／PKCE unit 2/2、112-ID一致性、正式碼／credential掃描及`git diff --check`皆通過。Google近版介面的選取控制與主要按鈕仍須依實際畫面辨認，不能用過時選單名稱推定。本輪不需migration、程式修改或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。下一步只讀回報勾選控制／目前狀態與主要按鈕文字，不點擊。
- YouTube scope選取控制續作開工（2026-08-09）：使用者提供目前Google權限頁截圖，未另貼email或OAuth網址；Codex目視確認頁面只有先前驗證的兩項唯讀YouTube權限，頂部有「全選」勾選框，兩個個別勾選框與「全選」目前皆未勾選，底部動作為「取消」與「繼續」。畫面另提示看不到`life-manager.workers.dev`的隱私權政策／服務條款連結，先如實記錄，不把它誤標成scope或目前授權成功。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration、程式修改或部署；下一步只按一次「全選」以選定這兩項已驗證唯讀權限，不按「繼續」。
- YouTube scope選取控制續作結果（2026-08-09）：真實截圖確認Google現行granular consent介面與控制狀態，沒有隱藏的第三項scope，也沒有預先勾選；因此「全選」在此畫面只會選定已逐項驗證的`youtube.readonly`與`yt-analytics.readonly`。隱私權政策／服務條款連結缺少提示另列為Testing畫面證據，現階段不把它當作已解決或真實授權阻擋，長期`In production`前須重新評估。截圖未複製進專案；112-ID一致性、正式碼／credential掃描及`git diff --check`皆通過。本輪不需migration、程式修改或部署；尚未選定或提交scope，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。下一步只按一次「全選」，確認頂部與兩個個別勾選框皆已選，不按「繼續」。
- YouTube兩項scope選定續作開工（2026-08-09）：使用者回覆「三個勾選框都已選取」，證明Google現行granular consent頁的「全選」及兩個個別唯讀權限皆已選，且畫面沒有第三項scope；使用者尚未按「繼續」，因此仍無callback、token或連線。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，並核對正式callback對scope、PKCE、state、token密文、connection與導回頁面的固定行為；不需migration、程式修改或部署。下一個平台操作只按一次Google「繼續」提交這兩項scope，回到App後停下，不同時按同步。
- YouTube兩項scope選定續作結果（2026-08-09）：Google真實畫面的「全選」與兩個個別唯讀scope已全部選定，沒有額外權限；使用者尚未提交。正式callback會驗證未過期且未消耗的state、redirect URI與S256 verifier，以client secret交換token，再逐項驗證Google實際回傳scope；只有兩項都存在才以AES-GCM-256密文保存access／refresh token、取得真實頻道identity與原始account payload、建立`CONNECTED` connection及`READY`同步job，最後303導回`/integrations?provider=youtube&connected=1`。它不會在同一步同步影片／Analytics內容。Worker/D1與callback契約16/16、112-ID一致性、正式碼／credential掃描及`git diff --check`皆通過；`rrule`只有既知缺少sourcemap來源警告。本輪不需migration、程式修改或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。下一步只按一次Google「繼續」，等待回到App後確認`CONNECTED`與「立即同步」，不按同步、不回報私人頻道名稱。
- YouTube callback `OAUTH_STATE_INVALID`診斷開工（2026-08-09）：使用者按Google「繼續」後回到真實staging App，但截圖顯示紅色「授權未完成：OAUTH_STATE_INVALID」、YouTube仍為「尚未授權頻道」，沒有`CONNECTED`或「立即同步」；截圖未提供token／state／頻道名稱。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定核對`src/worker/api/oauth.ts`的state TTL／失效條件、以Wrangler對staging D1執行唯讀聚合，只查YouTube state總數、過期／已消耗筆數與建立／到期時間，不選取state hash、PKCE密文、authorize URL、token或私人payload；必要時補固定時間測試與使用者可理解的重試文件。不得把重新授權當作修復證據，先確認根因；不需migration，是否需程式修改依診斷結果決定。
- YouTube callback `OAUTH_STATE_INVALID`診斷結果（2026-08-09）：正式程式把state TTL寫死為10分鐘，callback對找不到、已消耗或到期state統一安全回跳`OAUTH_STATE_INVALID`。Wrangler以binding `LIFE_DB`查詢在SQL前被Cloudflare API `7403`拒絕，沒有讀寫；依既有操作證據改用正式資料庫名稱`life-manager-staging`重試同一個唯讀聚合成功。D1只有1筆YouTube state：未消耗1、已消耗0、過期未消耗1，建立`08:08:09Z`、到期`08:18:09Z`、TTL固定10分鐘，查詢時已逾期35.9分鐘；`CONNECTED` YouTube連線0、YouTube原始payload 0，`rows_written=0`、`changes=0`。因此根因已確證為逐頁人工驗收超過10分鐘，不是Secret、redirect、scope或token半成品。截圖未複製進專案。
- YouTube OAuth state TTL修正開工（2026-08-09）：正式需求只要求OAuth state／PKCE／CSRF與redirect嚴格驗證，未規定10分鐘；本專案又強制每次使用者平台操作分步停下，現有寫死值與驗收流程衝突。維持隨機state、S256 PKCE、AES-GCM verifier、一次性消耗及到期拒絕不變，將TTL改成公開設定`OAUTH_STATE_TTL_MINUTES=60`並做整數／安全範圍驗證，讓單次分步驗收可完成但不形成無限期state。預定修改`src/worker/env.ts`、`src/worker/api/oauth.ts`、`wrangler.toml`、`vitest.worker.config.ts`、`tests/worker/env.d.ts`、`tests/worker/api-d1.test.ts`、`docs/ARCHITECTURE.md`、`docs/OPERATIONS.md`、`docs/SETUP_CHECKLIST.md`及本狀態證據；不需migration。預定測試為固定60分鐘D1答案、缺失／越界設定拒絕、既有callback安全契約、lint、雙typecheck、unit、Worker/D1、client build、正式碼／credential／跳過測試掃描與112-ID一致性；本機Playwright仍受已記錄的Windows Wrangler／workerd啟動阻擋，不得因此冒稱完整E2E通過。外部閘門為重新部署staging後在60分鐘內重跑本人真實Google授權。
- YouTube OAuth state TTL修正結果（2026-08-09）：`src/worker/api/oauth.ts`已改由`OAUTH_STATE_TTL_MINUTES`計算到期時間，缺失、非整數及10至120分鐘範圍外設定一律以503 `OAUTH_CONFIGURATION_MISSING`拒絕；正式staging值為60分鐘。Worker/D1固定答案直接呼叫真實authorize API並核對D1最新state的`expires_at-created_at=3,600,000ms`，同時保留隨機state、S256 PKCE、AES-GCM verifier、一次性消耗、嚴格redirect及逾期拒絕。`npm run lint`、雙typecheck、27/27 unit、17/17 Worker/D1、client build、正式碼／credential／skip掃描、112-ID一致性及`git diff --check`全部exit 0；`rrule`缺少sourcemap來源與client chunk大於500kB為既知警告。沒有新增或修改migration。本機完整Playwright未重跑，因既有Windows Wrangler／workerd啟動阻擋仍在，不以其他測試冒充E2E。Wrangler 4.118.0從中文實體路徑dry-run時重現Node 24 `UV_HANDLE_CLOSING`斷言；將同一工作樹暫時映射為ASCII磁碟代號後，dry-run與正式deploy均exit 0，映射已解除。Cloudflare部署版本`f646aea5-eef4-426f-b2af-3bf9f67975a1`取得100%流量，8個assets、staging D1與`OAUTH_STATE_TTL_MINUTES=60`綁定通過；四個必要Secret名稱／`secret_text`型別仍存在且未讀值，未登入health維持302。`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`；下一步只能從App建立全新授權state，不得重播舊Google頁或callback。
- YouTube 60分鐘新state續作開工（2026-08-09）：使用者已在目前staging外部連線頁只按一次「開始正式授權」，並回報新的Google授權頁已開啟；沒有重新整理、重播舊callback、選帳號或按Google按鈕，也未提供email、頻道名稱或含OAuth參數的網址。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定以Wrangler對staging D1執行唯讀聚合，只核對最新YouTube state的筆數、未消耗、尚未過期與`expires_at-created_at`分鐘數，不選取state hash、PKCE／authorize URL密文、token或私人payload；預定修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration或部署。外部閘門仍是本人test user、兩項唯讀scope、callback與首次同步，後續一次只做一個Google操作。
- YouTube 60分鐘新state續作結果（2026-08-09）：Wrangler 4.118.0以正式資料庫名稱`life-manager-staging`執行單一唯讀聚合，成功確認YouTube state總數2、最新資料1筆、未消耗1、尚未過期1、`expires_at-created_at=60.0`分鐘，查詢當下剩餘58.2分鐘；Cloudflare由APAC／HKG primary服務，`rows_written=0`、`changes=0`。SQL只選取計數、布林條件與分鐘差，未讀取state hash、PKCE／authorize URL密文、token、帳號或任何私人payload。這同時證明新頁不是舊state重播且staging修正已真實生效；本步不需migration、程式修改或部署。`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`；下一步只讀辨認新Google頁面主標題，不點擊。
- YouTube新流程帳號選擇頁續作開工（2026-08-09）：使用者保持新Google頁面不動，只讀回報主標題為「選擇帳戶」；尚未選帳號、按按鈕或提供email／網址／頻道名稱。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration、程式修改、D1查詢或部署；下一個平台操作只選擇先前加入Test users且管理本人YouTube頻道的同一Google帳號，後續畫面到達即停下。
- YouTube新流程帳號選擇頁續作結果（2026-08-09）：真實新OAuth流程已到Google「選擇帳戶」頁，且使用者在回報前未做任何選擇；此證據已寫入設定清單，不保存候選帳號、email或其他私人識別。本步不需migration、程式修改、D1查詢或部署。文件差異與112-ID狀態一致性通過後，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`；下一步只選擇先前加入Test users且管理本人頻道的同一帳號，下一頁出現即停下。
- YouTube新流程本人帳號選擇續作開工（2026-08-09）：使用者已在Google「選擇帳戶」頁只選擇先前加入Test users且管理本人YouTube頻道的同一帳號，下一頁主標題為「這個應用程式未經 Google 驗證」；未按警告頁按鈕，也未提供email、網址或頻道名稱。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration、程式修改、D1查詢或部署；下一個平台操作只按現行警告頁的「繼續」一次，到權限頁即停下。
- YouTube新流程本人帳號選擇續作結果（2026-08-09）：真實60分鐘新state流程已通過本人test user選擇，Google呈現與第一次相同的Testing App未驗證警告；這符合私人單一使用者測試策略，但不代表權限已授予。設定清單只記錄去敏主標題，不保存帳號識別。本步不需migration、程式修改、D1查詢或部署。文件差異、正式碼／Secret／skip掃描與112-ID一致性通過後，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`；下一步只按警告頁現有「繼續」一次，到權限頁即停下。
- YouTube新流程權限頁續作開工（2026-08-09）：使用者已在未驗證App警告頁只按一次「繼續」並提供下一頁截圖；Codex以原始解析度目視確認Google granular consent頁只列出「查看您的 YouTube 帳戶」與「查看依據您的 YouTube 內容產生的 YouTube 數據分析報表」兩項既定唯讀權限，「全選」及兩個個別勾選框目前皆未勾選，底部為「取消」與「繼續」。畫面仍提示缺少隱私權政策／服務條款連結；不把此提示當成已解決。截圖未複製進專案或Git，帳號區域未轉錄。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration、程式修改、D1查詢或部署；下一個平台操作只按一次「全選」，不按「繼續」。
- YouTube新流程權限頁續作結果（2026-08-09）：60分鐘新state已到達與第一次一致的Google granular consent頁；真實畫面再次證明只有`youtube.readonly`與`yt-analytics.readonly`對應的兩項唯讀能力，沒有收益、上傳、編輯、刪除、管理或隱藏的第三項scope，也沒有預先勾選。缺少隱私權政策／服務條款連結提示繼續保留為Testing證據，未標成已解決。截圖未複製進專案。本步不需migration、程式修改、D1查詢或部署。文件差異、正式碼／Secret／skip掃描與112-ID一致性通過後，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`；下一步只按一次「全選」並確認三個勾選框已選，不按「繼續」。
- YouTube新流程兩項scope選定續作開工（2026-08-09）：使用者已在60分鐘新state的Google granular consent頁只按一次「全選」，並確認「全選」與兩個個別唯讀權限共三個勾選框皆已選取；尚未按「繼續」，因此仍未提交scope、觸發callback或建立connection。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`，不需migration、程式修改、D1查詢或部署；下一個平台操作只按一次Google「繼續」，回到App即停下，不按「立即同步」。
- YouTube新流程兩項scope選定續作結果（2026-08-09）：真實60分鐘新state流程的「全選」與兩個個別唯讀scope已全部選定，沒有額外權限；使用者尚未提交，因此目前沒有新的callback、token、connection或同步資料。本步不需migration、程式修改、D1查詢或部署。文件差異、正式碼／Secret／skip掃描與112-ID一致性通過後，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`；下一步只按一次Google「繼續」，自動回到App後確認有無紅色錯誤、`CONNECTED`與「立即同步」，但不按同步。
- YouTube新流程callback成功續作開工（2026-08-09）：使用者已只按一次Google「繼續」並回到真實staging App，回報沒有紅色錯誤、YouTube顯示`CONNECTED`且出現「立即同步」；Codex以原始解析度目視確認卡片為`CONNECTED`、最近錯誤「無」、sync job為`READY`／第0次，並有「立即同步」與「撤銷連線」操作。頻道名稱區域已模糊，Codex未轉錄；截圖未複製進專案或Git。使用者尚未按「立即同步」。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定以Wrangler對staging D1執行去敏唯讀聚合，只核對最新state已消耗、CONNECTED筆數、兩項scope完整且無額外scope、AES-GCM token欄位存在、account原始payload筆數與READY job／attempt；不選取帳號ID、顯示名稱、token、payload或其他私人內容。預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`與`docs/TRACEABILITY_MATRIX.md`，不需migration、程式修改或部署；真實內容同步仍為下一個獨立驗收步驟。
- YouTube新流程callback成功核對與排程競態結果（2026-08-09）：第一個staging D1唯讀聚合確認最新state已消耗、access／refresh token密文與`AES-GCM-256`存在、兩項唯讀scope精確吻合且無額外scope、account `channels`原始payload 1、callback成功run 1；`rows_written=0`、`changes=0`。但截圖後15分鐘排程已自動接手，故查詢當下不再是`CONNECTED`／`READY`。第二個唯讀聚合確認目前connection=`ERROR`、job=`RETRY`第1次、非`NEEDS_REAUTH`，scheduled run失敗1，尚無playlist／videos／analytics原始payload；第三個唯讀查詢只比對已去敏訊息，定位為`PROVIDER_ERROR`／`analytics`階段。這證明OAuth與Data API account讀取成功，但尚未完成真實內容同步，不能以截圖瞬間狀態標`VERIFIED`。
- YouTube Analytics有效查詢與正規化修正開工（2026-08-09）：Google官方Channel Reports現行規格的基本時間報表要求`dimensions`恰為`day`或`month`其中一個，`video`只能作該報表的篩選；現有`dimensions=video,day`／`sort=day,video`不符合允許組合，且`persistYouTube`完全未正規化Analytics rows。正式需求`SOC-009`要求「可用Analytics指標」、原始回應與正規化快照，故不以移除Analytics、忽略錯誤或只改狀態冒充修復。預定修改`src/integrations/youtube/provider.ts`（官方有效`day`頻道報表、Data API完整分頁、來源定義版本）、`src/worker/api/provider-sync.ts`（Pacific來源日界、帳號級每日views／likes／comments非累積快照、成功後更新來源定義版本）、`tests/unit/oauth-providers.test.ts`、`tests/worker/api-d1.test.ts`、`docs/ARCHITECTURE.md`、`docs/DATA_AND_SYNC.md`、`docs/OPERATIONS.md`、`docs/SETUP_CHECKLIST.md`、`docs/TRACEABILITY_MATRIX.md`、`docs/REFERENCES.md`及本狀態證據；不需migration。預定固定答案為有效query參數、重複pageToken拒絕、超過單頁影片不截斷、Pacific夏／冬日界、三項日指標的名稱／值／非累積／raw provenance D1列；再跑lint、雙typecheck、unit、Worker/D1、client build、正式碼／credential／skip掃描、112-ID一致性與Wrangler staging dry-run／部署。外部閘門為部署後讓RETRY或本人手動同步真實成功，再做去敏D1聚合；未成功前`SOC-009`與`SETUP-003`維持`IN_PROGRESS`。
- YouTube Analytics快照唯一性規格衝突補記（2026-08-09）：實作前核對`0004_social_integrations.sql`發現原`UNIQUE(social_metric_definition_id, social_account_id, platform_post_id, observed_at, source_type)`同時包含兩個XOR可空外鍵；SQLite的`NULL`值不互相衝突，故帳號級與貼文級快照皆不能靠此約束保證idempotency。只做應用層先查再寫仍有並行競態，不符合正式寫入規則。前段「不需migration」是開工預估，現由實際schema證據更正：新增`0009_social_snapshot_uniqueness.sql`，分別建立帳號級與貼文級partial unique index並將schema metadata升為9；既有`0001`～`0008`不得修改。套用staging前先以只讀聚合確認兩類重複群組皆為0，不刪除或改寫任何既有資料；fresh與8→9 upgrade、D1固定答案及完整閘門必須通過後才可remote apply。
- YouTube排程續期與錯誤重試UI衝突補記（2026-08-09）：`provider_connections`已保存加密refresh token與access token到期時間，YouTube adapter也已實作`refreshCredentials`，但`syncProviderConnection`直接使用到期access token，從未呼叫換發；另一方面`IntegrationsPage`把所有非`CONNECTED`狀態都只顯示「開始正式授權」，因此本次非401 Analytics錯誤轉為`ERROR`後，使用者重新整理即無法用既有有效授權手動重試。這兩點與長期排程及狀態語意衝突，不能靠再次OAuth掩蓋。預定追加修改`src/worker/api/oauth.ts`、`src/worker/api/provider-sync.ts`、`src/app/pages/IntegrationsPage.tsx`與對應unit／Worker測試：到期前安全換發並重新加密保存token、保留既有refresh token、換發失敗才標`NEEDS_REAUTH`；`ERROR`保留「立即同步」，只有缺少連線或`NEEDS_REAUTH`／`EXPIRED`才要求授權。外部阻擋不變，仍需修正部署後的真實同步成功與去敏D1證據。
- YouTube修正本機閘門與staging migration預檢（2026-08-09）：有效Analytics query、52支影片兩頁／兩批固定答案、重複page token拒絕、Pacific夏／冬日界、三項日指標D1 provenance與partial unique index、到期前token換發／密文回存／refresh token保留，以及`ERROR`保留手動同步的測試均已加入。`npm run lint`、雙typecheck、35個unit、19個Worker/D1、client production build與正式碼／Secret／skip掃描通過；完整Playwright仍受既載明的Windows本機Wrangler提前退出阻擋，沒有改用簡化伺服器冒充通過。staging migration list精確只有`0009_social_snapshot_uniqueness.sql`；正式資料庫名稱的第一個並行唯讀query在SQL前遇到Cloudflare `7403`，改用已設定的`LIFE_DB` staging binding唯一重試成功，APAC／HKG primary回報schema 8、帳號級重複群組0、貼文級重複群組0、`rows_written=0`、`changes=0`。因此可在先建立不進Git的remote備份後安全執行8→9 migration；不得刪除或改寫快照。
- YouTube真實日指標符號性缺口（2026-08-09）：staging先完成不進Git的remote SQL備份（276,741 bytes；SHA-256 `89DC670CE4B8BEE42EDB1ABBC962029E3B3903847131AE5E28FFC3CC3E944506`），再成功套用`0009`；唯讀驗證為schema 9、兩個partial unique index皆存在且migration list為空。修正版Worker `d42a929d-5ec0-4cce-a7e4-a2ba37076a82`已100%部署，四個Secret名稱／型別完整，未登入health 302。新版本第一次真實Cron由`RETRY`第2次執行後仍失敗，去敏錯誤為`YouTube Analytics likes值無效`；只統計最新官方payload的型別／符號發現88列皆為日期文字與三個整數，其中views負值0列、likes負值9列、comments負值0列，未讀取日期或實際值。現有正規化器錯把所有區間指標限制為非負，與真實來源回報衝突；不得把負值硬改為0或捨棄列。預定修改`youtubeAnalyticsDailyPoints`接受有限的帶符號十進位來源值、保留`SOURCE_REPORTED`與原值，增加負likes固定答案及D1密切測試，再完整重跑與部署；`SOC-009`／`SETUP-003`維持`IN_PROGRESS`。
- YouTube帶符號來源值修正與部署結果（2026-08-09）：正規化器現接受有限的帶符號十進位並原樣保存，負likes固定答案及D1密切測試證明`-2`不會被改成0或捨棄。`npm run lint`、雙typecheck、35/35 unit、19/19 Worker／D1、client production build與正式碼／Secret／skip掃描均通過；Worker版本`f8a9750b-af08-4a27-91d7-679f725ddfea`已100%部署，Secret名稱／型別與未登入Access 302仍完整。其後`npm run test:e2e`在Wrangler 4.118.0真實啟動本機Worker，以每案例全新D1套用`0001`～`0009`，12/12 Playwright全部通過、exit 0；先前相同Windows環境的啟動中斷是歷史失敗，不再是目前阻擋。
- YouTube修正版真實Cron驗收結果（2026-08-09）：既有`RETRY`第3次在staging完成，最新run=`SUCCEEDED`／trigger=`RETRY`／error 0，job回到`READY`／attempt 0，connection=`CONNECTED`且錯誤清空，來源版本為`youtube-data-v3+analytics-v2@2026-08-09`。去敏唯讀D1聚合確認1個真實頻道、31支影片；原始payload為channels 1、playlist_items 2、videos 2、analytics 1；三項帳號日指標各88筆，五項影片累積指標各62筆，總快照574，raw provenance缺失0、帳號級／貼文級相同語意鍵重複群組皆0。88筆daily likes中9筆為來源負向調整並完整保留。所有查詢由APAC／HKG primary服務且`rows_written=0`，未選取頻道ID、名稱、影片標題、日期、實際指標值、token或payload內容。真實API讀取與D1正規化部分通過；`AT-YT-04`仍需本人UI及YouTube Studio選定期間人工核對，`AT-YT-05`仍需撤銷token後驗收，故`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。
- YouTube同步後UI核對開工（2026-08-09）：使用者依指示只重新整理目前staging「外部連線」頁，未按「立即同步」或「撤銷連線」，並以三個「是」逐項確認YouTube仍為`CONNECTED`、最近錯誤為「無」、排程為`READY`／第0次。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`、`docs/TRACEABILITY_MATRIX.md`與`docs/REFERENCES.md`；不需migration、程式修改、D1寫入或部署。預定檢查為文件差異、需求狀態一致性、正式碼／Secret／skip掃描；外部閘門縮小為AT-YT-04的YouTube Studio人工指標核對，仍一次只要求一個平台操作。
- YouTube同步後UI核對結果（2026-08-09）：本人重新整理後的真實staging頁仍顯示`CONNECTED`、最近錯誤「無」及下一排程`READY`／第0次，證明成功狀態不是callback瞬間畫面，也沒有被舊錯誤或RETRY狀態覆蓋；使用者未觸發額外同步或撤銷。官方YouTube Help同時註明2026年7月起逐步推出新版Studio，Analytics頁可能不同，因此後續設定清單改用`studio.youtube.com`、本人頻道與channel-level Analytics成功判據，不把單一側欄文字當成唯一入口。本步不需migration、程式修改、D1查詢或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。
- YouTube Studio本人頻道入口開工（2026-08-09）：使用者依指示在電腦開啟`https://studio.youtube.com/`，回覆「Studio 首頁已開啟」，代表管理本人頻道的同一Google帳號可到達正確頻道的Studio首頁；尚未進入Analytics、切換日期、匯出或回報任何私人指標值。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`與`docs/TRACEABILITY_MATRIX.md`；不需migration、程式修改、D1查詢或部署。預定檢查為文件差異、需求狀態一致性及正式碼／Secret／skip掃描；下一外部閘門只進入頻道層級Analytics頁，不同時設定日期或指標。
- YouTube Studio本人頻道入口結果（2026-08-09）：本人正確頻道的Studio首頁已在電腦開啟，沒有登入、帳號切換或頻道選擇阻擋；尚未把首頁概覽數字當成AT-YT-04核對，也未回報私人數值。本步不需migration、程式修改、D1查詢或部署。文件證據收斂後，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`；下一步只依實際Studio介面進入頻道層級數據分析頁，頁面開啟即停下。
- YouTube Studio頻道Analytics入口開工（2026-08-09）：使用者已在本人正確頻道的Studio首頁只點擊通往頻道分析的入口，回覆「數據分析頁已開啟」；尚未切換日期、頁籤、進階模式或回報任何指標值。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`與`docs/TRACEABILITY_MATRIX.md`；不需migration、程式修改、D1查詢或部署。預定檢查為文件差異、需求狀態一致性及正式碼／Secret／skip掃描；下一外部閘門只辨認目前頁面是否提供「進階模式」或「查看更多」深入報表入口，不先點擊。
- YouTube Studio頻道Analytics入口結果（2026-08-09）：本人正確頻道的channel-level Analytics頁已開啟，尚未變更預設報表條件或揭露私人數值；這完成AT-YT-04人工核對的正確帳號／頻道／報表層級前置條件，但尚未完成日期與指標對照。本步不需migration、程式修改、D1查詢或部署。文件證據收斂後，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`；下一步只讀辨認目前頁面是否顯示「進階模式」或任一報表的「查看更多」，不點擊。
- YouTube Studio深入報表入口文字開工（2026-08-09）：使用者保持channel-level Analytics頁不動，只讀確認頁面可見「進階模式」，且各報表卡片的實際文字皆為「顯示更多」，沒有「查看更多」；尚未點擊、切換日期／頁籤或回報任何指標值。這更正設定文件依官方英語`See more`直譯成「查看更多」的介面假設。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`；不需migration、程式修改、D1查詢或部署。預定檢查為文件殘留文字、需求狀態一致性與正式碼／Secret／skip掃描；下一外部操作只點擊已確認存在的「進階模式」。
- YouTube Studio深入報表入口文字結果（2026-08-09）：目前本人頻道Analytics頁的可用全頁入口確定為「進階模式」，卡片入口確定為「顯示更多」；設定清單已採真實中文文字，不以舊譯名「查看更多」指示使用者。為統一控制日期、維度與指標，後續選擇「進階模式」，不從任一卡片的「顯示更多」進入。本步不需migration、程式修改、D1查詢或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。
- YouTube Studio進階模式入口開工（2026-08-09）：使用者依指示只點擊一次已確認存在的「進階模式」，回覆「進階模式已開啟」；沒有點「顯示更多」、切換日期、維度、指標、篩選、匯出或回報數值。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`；不需migration、程式修改、D1查詢或部署。預定檢查為文件差異、需求狀態一致性與正式碼／Secret／skip掃描；下一外部步驟只讀辨認目前進階模式的日期範圍控制文字，不點擊。
- YouTube Studio進階模式入口結果（2026-08-09）：本人頻道的進階分析報表已成功開啟，尚未變更任何控制或揭露私人數值；後續可在同一報表內統一設定日期與指標，不需要依賴卡片入口。本步不需migration、程式修改、D1查詢或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。下一步只查看日期範圍控制目前顯示的文字，先確認現行介面再操作。
- YouTube Studio同期間觀看次數核對開工（2026-08-09）：使用者提供目前進階模式截圖，畫面顯示日期控制為2026/7/12至2026/8/8／最近28天，報表為依內容顯示觀看次數，總計可見；內容名稱區域不轉錄，截圖不複製進專案或Git。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定以staging D1執行單一唯讀聚合，只回傳該期間daily views列數、raw provenance缺失數與「總和是否精確等於Studio畫面總計」布林值，不輸出實際總和、逐日值、內容ID／名稱、token或payload；預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`與`docs/TRACEABILITY_MATRIX.md`，不需migration、程式修改或部署。若精確相等，AT-YT-04真實頻道／影片／一項Analytics／人工核對可通過；若不等，保留`IN_PROGRESS`並診斷日期、時區與定義差異。
- YouTube Studio第一輪觀看次數核對結果（2026-08-09）：兩次staging D1唯讀聚合均成功且`rows_written=0`／`changes=0`；Studio畫面範圍為2026/7/12至2026/8/8，但API daily views目前只涵蓋2026/7/12至2026/8/6，共26列／26個來源日期，raw provenance缺失0，因此精確相等布林值為0。這是Studio尾端另含8/7及8/8、API尚未回傳相同日期所致，不是正規化列或原始證據遺失；未輸出或記錄實際總和、逐日值、內容ID／名稱、token或payload。`AT-YT-04`尚未通過；本次診斷與文件收斂完成後，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`。下一外部操作只把Studio自訂日期對齊為2026/7/12至2026/8/6，不同時變更其他報表條件或回報總計。
- YouTube Studio第二輪觀看次數核對開工（2026-08-09）：使用者已把進階模式日期設為自訂2026/7/12至2026/8/6並提供截圖；畫面仍為依內容顯示觀看次數且總計可見，內容名稱區域不轉錄，截圖不複製進專案或Git。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定以staging D1執行單一唯讀聚合，只回傳26天daily views列數、raw provenance缺失數與「總和是否精確等於Studio畫面總計」布林值，不輸出或寫入實際總和、逐日值、內容ID／名稱、token或payload；預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`與`docs/TRACEABILITY_MATRIX.md`，不需migration、程式修改、部署或D1寫入。預定檢查為需求112 ID一致性、正式碼／Secret／skip掃描及文件差異；若精確相等則完成`AT-YT-04`，若不等則保留未通過並診斷定義差異。外部阻擋仍為`AT-YT-05`撤銷重連與Google App長期refresh token狀態。
- YouTube Studio第二輪觀看次數核對結果（2026-08-09）：staging D1唯讀聚合回傳2026/7/12至2026/8/6 daily views 26列／26個不同來源日、精確起訖日期=1、raw provenance缺失0、Studio總計精確相等=1；由APAC／HKG primary服務，`rows_written=0`／`changes=0`。API `day`採Pacific來源日，Studio人工核對使用相同印出日期，不做Asia/Taipei位移；實際總和、逐日值、內容ID／名稱、token與payload均未寫入文件或查詢輸出。結合既有成功Cron的1真實頻道、31影片及Analytics原始／正規化證據，`AT-YT-04`通過。本輪不需migration、程式修改或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，仍不得標`VERIFIED`，因Google App長期refresh token狀態與`AT-YT-05`撤銷重連尚未完成。
- Google Auth Platform長期自用發布介面核對開工（2026-08-09）：使用者已在Project ID `life-manager-personal-505006`的Audience頁只讀回報Publishing status為「測試」，可用動作為「發布應用程式」；沒有按鈕、切換狀態或提交驗證，也未提供email、client ID或secret。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`；不需migration、程式修改、D1、部署或secret操作。預定檢查為112-ID狀態一致性、正式碼／Secret／skip掃描與文件差異；外部阻擋是使用者須先只開啟Google現行發布確認視窗，再依視窗真實文字決定單一步驟，不同時提交公開驗證。
- Google Auth Platform長期自用發布介面核對結果（2026-08-09）：Audience頁真實狀態確定為「測試」，且目前可用動作確定為「發布應用程式」；這與Testing refresh token七日到期、長期自用需切換`In production`的正式文件一致，但尚未按下任何狀態變更。此步不需migration、程式修改、D1、部署或secret操作；文件與掃描收斂後，`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，未標`VERIFIED`。下一步只按一次「發布應用程式」開啟確認視窗，先核對當下介面文字，不在同一步確認發布或提交公開驗證。
- Google Auth Platform發布確認視窗核對開工（2026-08-09）：使用者已只按一次「發布應用程式」並提供確認視窗截圖，尚未按「取消」或「確認」。Codex以原始解析度核對標題、通用存取／驗證說明及兩個動作按鈕；截圖不複製進專案或Git，帳號區域不轉錄。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定只修改`docs/IMPLEMENTATION_STATUS.md`與`docs/SETUP_CHECKLIST.md`；不需migration、程式修改、D1、部署或secret操作。預定檢查為112-ID狀態一致性、正式碼／Secret／skip掃描與文件差異；外部阻擋是使用者須確認發布後再只讀核對狀態，禁止在同一步進入或提交驗證中心。
- Google Auth Platform發布確認視窗核對結果（2026-08-09）：真實視窗標題為「要推送至正式環境嗎？」；通用說明指出所有Google帳戶可存取，並列出超過10個網域、設有標誌或要求機密／受限制scope時需送交驗證；按鈕為「取消」與「確認」。此通用視窗未呈現官方「個人自用、少量已知使用者可不提交公開驗證」例外，但不代表驗證已提交或本產品需建立公開onboarding；本專案仍以Cloudflare Access單一email與App內無公開連接流程維持私人邊界，接受未驗證警告／使用者上限。此步不需migration、程式修改、D1、部署或secret操作；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`。下一步只按「確認」切換發布狀態，更新後先只讀核對，不進入驗證中心。
- Google Auth Platform長期自用發布完成與AT-YT-05預檢開工（2026-08-09）：使用者已在確認視窗按「確認」並提供更新後Audience頁截圖；Codex以原始解析度確認Publishing status為「實際運作中」、可用動作為「返回測試應用程式」，另有「前往驗證中心」通用提示但未點擊。截圖不複製進專案或Git，帳號區域不轉錄。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。本輪預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`、`docs/TRACEABILITY_MATRIX.md`及修正`docs/OPERATIONS.md`的舊AT-YT-04敘述；不需migration、runtime程式、D1、部署或secret操作。另唯讀核對App「撤銷連線」實作、排程選取與失效狀態，確認下一步不會誤用Google驗證中心；預定檢查為112-ID一致性、正式碼／Secret／skip掃描與文件差異。外部阻擋只剩使用者逐步完成AT-YT-05撤銷、排程錯誤隔離及重新授權。
- AT-YT-05自動證據缺口（2026-08-09）：唯讀核對確認App「撤銷連線」會呼叫`https://oauth2.googleapis.com/revoke`、成功後清除兩種token密文並標記`DISCONNECTED`，而既有job仍保留供排程失敗隔離驗收；但`tests/unit/oauth-providers.test.ts`尚無直接固定答案證明撤銷會優先使用refresh token與官方端點。真實撤銷前先追加這一個adapter測試，不修改runtime、migration或遠端資源；預定重跑該unit、完整unit、需求一致性、掃描及文件差異。API狀態轉換與排程錯誤將由後續真實staging操作及D1去敏聚合補足，不以單元測試冒充外部完成。
- Google Auth Platform長期自用發布完成與AT-YT-05預檢結果（2026-08-09）：Audience真實Publishing status已為「實際運作中」，狀態動作為「返回測試應用程式」；「前往驗證中心」未點擊，沒有建立公開onboarding或提交公眾驗證。程式唯讀稽核確認App「撤銷連線」實際呼叫Google官方revoke端點、成功後清除access／refresh token密文並標記`DISCONNECTED`，既有job保留供排程錯誤隔離驗收；新增固定答案證明優先撤銷refresh token且不送出短期access token。目標unit 7/7、完整unit 36/36、lint與雙typecheck均exit 0；不需migration、runtime修改、D1、部署或secret操作。長期自用發布完成，但現有token是在Testing時期簽發，為排除七日限制風險並完成正式AT-YT-05，仍須真實撤銷後於「實際運作中」重新授權；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`。
- AT-YT-05真實撤銷前基準開工（2026-08-09）：使用者已回到並重新整理staging外部連線頁；截圖顯示YouTube=`CONNECTED`、最近錯誤「無」、job=`READY`／attempt 0，且可見「立即同步」與「撤銷連線」，尚未按任何操作。頻道名稱已模糊且不轉錄，截圖不複製進專案或Git。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定先以staging D1執行單一唯讀聚合，只回傳YouTube連線數／CONNECTED數、access／refresh token密文存在布林、job READY／attempt及三個財務核心表筆數；不選取token、帳號ID、名稱、財務金額或內容。預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`與必要的`docs/OPERATIONS.md`；不需migration、runtime修改或部署。基準、112-ID一致性、掃描及文件差異通過後，才要求使用者只按一次「撤銷連線」；外部阻擋為後續排程錯誤隔離、財務UI正常及重新授權。
- AT-YT-05真實撤銷前基準結果（2026-08-09）：staging頁人工基準為YouTube=`CONNECTED`、錯誤「無」、job=`READY`／attempt 0，並可見「立即同步／撤銷連線」。Wrangler去敏唯讀D1聚合為有效YouTube connection 1／CONNECTED 1、access密文存在1、refresh密文存在1、YouTube job 1／READY 1／max attempt 0；三個財務核心表目前皆0列。查詢由APAC／HKG primary服務且`rows_written=0`／`changes=0`，未讀取token、帳號識別、名稱、財務金額或內容。本輪不需migration、runtime修改或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`。下一步只按一次App「撤銷連線」，這會真實呼叫Google revoke並清除token密文，但保留歷史社群資料與其他模組；撤銷後先停下核對UI，不立即重連。
- AT-YT-05真實撤銷後核對開工（2026-08-09）：使用者在staging YouTube卡片只按一次「撤銷連線」並提供更新後截圖；畫面顯示connection=`DISCONNECTED`、操作改為「開始正式授權」、最近錯誤仍為「無」，且既有job仍顯示`READY`／attempt 0。頻道名稱已模糊且不轉錄，截圖不複製進專案或Git。`SOC-009`與`SETUP-003`暫改為`IN_PROGRESS`。預定以staging D1執行單一去敏唯讀聚合，只回傳連線狀態、兩種token密文存在布林、job筆數／狀態、既有YouTube原始payload與正規化快照筆數，以及三個財務核心表筆數；不選取token、帳號ID、名稱、標題、payload、財務金額或內容。預定修改`docs/IMPLEMENTATION_STATUS.md`、`docs/SETUP_CHECKLIST.md`與`docs/OPERATIONS.md`，不需migration、runtime修改或部署。外部阻擋仍為排程失敗隔離證據、財務UI正常及於Google App「實際運作中」狀態重新授權。
- AT-YT-05撤銷後資料庫基準結果（2026-08-09）：第一次以binding名稱執行的SQL在查詢前被Cloudflare API `7403`拒絕，沒有讀寫資料；依既有成功路徑改用正式D1名稱唯一重試後，去敏唯讀聚合成功。connection總列1／`DISCONNECTED` 1／撤銷時間存在1、access與refresh token密文存在數皆0、YouTube job 1／`READY` 1／attempt 0、原始payload總數6、正規化快照574；三個財務核心表仍皆0列。APAC／HKG primary回報`rows_written=0`／`changes=0`。查詢未選取token、帳號識別、名稱、標題、payload、財務金額或內容，證明撤銷已清除密文且沒有刪除歷史資料或影響財務資料。Cloudflare現行正式文件只提供本機scheduled handler測試，沒有已部署Worker的文件化手動觸發；為取得真實staging Cron隔離證據，下一步只把此已斷線job的`next_run_at`提前到目前時間之前，等待既有每15分鐘Cron自然執行一次，不修改連線、憑證、歷史資料或其他job。
- AT-YT-05真實排程錯誤隔離結果（2026-08-09）：Codex只把唯一已斷線YouTube job的`next_run_at`提前，D1回報精確`rows_written=1`／`changes=1`；未修改連線、憑證、attempt、歷史資料或其他job。下一個既有staging Cron於21:15（Asia/Taipei）自然執行後，去敏唯讀聚合確認job=`RETRY`／attempt 1／`PROVIDER_ERROR` 1，connection仍為`DISCONNECTED`、兩種token密文存在數仍0；原始payload仍6、正規化快照仍574，三個財務核心表仍皆0列。核對由APAC／HKG primary服務且`rows_written=0`／`changes=0`。這是真實已部署Worker Cron失敗紀錄，不是本機scheduled模擬或假Google回應；失敗只推進該job重試狀態，沒有復活連線、刪除社群歷史或影響財務資料。完整`npm run verify`中的lint、雙typecheck、unit 36/36、Worker/D1 19/19、build、Playwright各情境及掃描皆通過，最終只因本階段依法仍標`IN_PROGRESS`而由requirements release-ready閘門回報非零。下一步由使用者只開啟staging「財務」頁，確認真實空資料畫面正常且無YouTube錯誤，之後才重新授權。
- AT-YT-05其他模組真實UI隔離結果（2026-08-09）：使用者在同一staging登入session只開啟左側「財務」，回覆頁面正常載入、正常顯示空資料狀態，且沒有YouTube或其他錯誤；未新增帳戶、交易、分類或匯入檔案，也未向Codex揭露任何私人財務內容。結合撤銷前後三個財務核心表皆0列的D1證據，證明YouTube撤銷與排程失敗未污染財務資料或畫面。本步不需migration、程式修改、部署或遠端寫入；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，因AT-YT-05仍須在Google App「實際運作中」狀態重新授權、真實同步並核對新refresh token密文與排程復原。
- AT-YT-05「實際運作中」重新授權核對開工（2026-08-09）：使用者回到外部連線頁時截圖顯示YouTube=`DISCONNECTED`、job已由真實Cron累積至`RETRY`／attempt 4、connection最近錯誤仍「無」，操作為「開始正式授權」。使用者只按一次授權入口，依Google現行介面選定同一本人帳戶，通過未驗證App的「進階」與「前往 life-manager.workers.dev（不安全）」警告，並只勾選YouTube帳戶與Analytics報表兩項唯讀scope後按「繼續」。callback回到staging後截圖顯示無紅色錯誤、YouTube=`CONNECTED`、job=`READY`／attempt 0、最近錯誤「無」，並可見「立即同步／撤銷連線」。帳號名稱已模糊且不轉錄，截圖不複製進專案或Git。`SOC-009`與`SETUP-003`改為`IN_PROGRESS`；預定以staging D1執行去敏唯讀聚合，只核對新access／refresh密文存在布林、兩項scope完整、token到期時間在未來、connection／job狀態、歷史社群筆數與財務筆數，不選取token、帳號識別、名稱、標題、payload或財務內容。本步不需migration、runtime修改或部署；外部阻擋為後續一次真實立即同步與長期refresh token狀態驗收。
- AT-YT-05「實際運作中」重新授權資料庫結果（2026-08-09）：staging D1去敏唯讀聚合確認有效`CONNECTED` connection 1、新access密文存在1、新refresh密文存在1、token到期時間在未來1、granted scope精確等於YouTube帳戶與Analytics報表兩項唯讀scope 1、近期MANUAL callback成功1、job 1且`READY`／attempt 0／無job錯誤1。撤銷前後的因果鏈為兩種密文`1→0→1`，因此目前密文不是沿用已撤銷舊值；查詢未讀取任何密文內容。原始payload仍6、正規化快照仍574，三個財務核心表仍皆0列；APAC／HKG primary回報`rows_written=0`／`changes=0`。重新授權與排程復原證據通過，本步不需migration、runtime修改或部署；`SOC-009`與`SETUP-003`恢復`AWAITING_USER_SETUP`，下一步只以目前新憑證按一次「立即同步」，不得用callback的單一頻道讀取冒充完整同步。
- AT-YT-05新憑證首次手動同步診斷開工（2026-08-09）：使用者只按一次YouTube卡片「立即同步」；更新後截圖顯示connection仍`CONNECTED`、最近嘗試／最近成功仍為22:04:02、最近錯誤「無」、下次排程仍`READY`／attempt 0且按鈕仍可用，但左下角全域同步區顯示紅色「錯誤／請求已取消」。這不是完整同步成功證據，也尚不能判定伺服器是否收到請求。`SOC-009`與`SETUP-003`改為`IN_PROGRESS`。預定唯讀檢查`IntegrationsPage` mutation、共用API client／AbortSignal、service worker與手動同步endpoint，再以staging D1去敏聚合核對近期run數／狀態與job；不輸出token、帳號識別、名稱、標題、payload或私人內容，不重複呼叫外部API。若D1沒有新run，診斷前端取消；若run存在，依真實狀態區分仍執行、成功或失敗，不以UI toast冒充結論。
- AT-YT-05首次手動同步診斷與修正結果（2026-08-09）：staging D1確認唯一新MANUAL run已收到請求並寫入4個raw payload、462筆快照，但超過1002秒仍停在`RUNNING`；22:15 Cron因舊手動流程未claim job而重疊啟動，該SCHEDULED run另行`SUCCEEDED`並寫入155筆快照，job回到`READY`／attempt 0，財務三表仍0。排程成功不算MANUAL成功。程式根因為provider長請求占用核心request gate使outbox的30秒AbortController顯示「請求已取消」、每筆快照重查definition及逐筆D1寫入、手動與Cron缺少單一claim。預定修改已完成：`src/app/api/client.ts`、`IntegrationsPage.tsx`分離長請求並停用pending動作；`provider-sync.ts`加入run內definition快取、每100筆D1 batch、10分鐘stale recovery及手動job claim；`scheduled/index.ts`先復原stale run再條件式claim；正式錯誤碼與3組固定答案測試同步更新。本輪不需migration。lint、雙typecheck、38/38 unit、20/20 Worker／D1、client build、`0001`～`0009`完整隔離D1 Playwright及正式碼掃描均通過；仍須部署staging並由本人重做一次MANUAL run，故維持`IN_PROGRESS`，不標`VERIFIED`。
- AT-YT-05同步修正staging部署結果（2026-08-09）：Wrangler上傳新`app.js`與Worker後雖在Windows回傳exit 1，部署輸出已給出版本`683e276f-8757-4d3b-8ee7-1d10defc5d8f`；另以`versions list`與`deployments status`交叉確認版本存在且承接100%流量，故不以單一退出碼猜測。四個Secret名稱／`secret_text`型別完整，未讀取值；未登入health為Access 302。部署後D1唯讀核對仍為connection=`CONNECTED`、access／refresh密文存在、無connection錯誤、job=`READY`／attempt 0，舊MANUAL仍`RUNNING`而後續SCHEDULED為`SUCCEEDED`；查詢`rows_written=0`。新程式會由下一次自然Cron或新MANUAL入口先把舊run收斂成`FAILED/SYNC_INTERRUPTED`。程式、部署與安全邊界已完成，外部阻擋只剩使用者載入新bundle及一次真實手動同步，`SOC-009`與`SETUP-003`改為`AWAITING_USER_SETUP`。
- AT-YT-05 stale run真實復原結果（2026-08-09）：新版本部署後第一個22:45自然Cron於22:45:13開始並在同秒完成；舊MANUAL run由`RUNNING`收斂為`FAILED`／`SYNC_INTERRUPTED`、error count 1與明確完成時間。該Cron的provider request／success／retry／dead-letter皆0，證明沒有到期job或額外外部API同步；現有job仍`READY`／attempt 0／無錯誤，connection仍`CONNECTED`／無錯誤。後續唯讀查詢由APAC／HKG primary回報`rows_written=0`／`changes=0`。自動stale recovery真實staging驗收通過，但它只終止中斷歷史，不能冒充修正版MANUAL同步成功；唯一外部阻擋不變。
- AT-YT-05修正版MANUAL重試開工（2026-08-09）：使用者依唯一操作重新整理staging外部連線頁，回報頁面正常且YouTube仍為`CONNECTED`；未按同步或撤銷。`SOC-009`與`SETUP-003`改為`IN_PROGRESS`。本輪不需migration或新程式修改；預定外部操作只有按一次YouTube「立即同步」並等待同一mutation終止。固定成功答案為按鈕在執行時顯示「同步中」且不可重複操作，完成後connection=`CONNECTED`、最近成功更新、最近錯誤無、job=`READY`／attempt 0、按鈕恢復「立即同步」、左下角無取消錯誤；之後由Codex以D1去敏唯讀查詢確認新的MANUAL run=`SUCCEEDED`、raw payload四類齊全、snapshot provenance／唯一性及財務隔離。若畫面先出錯或超過10分鐘仍未終止，立即停止，不以後續Cron結果冒充。
- AT-YT-05修正版MANUAL結果與追溯衝突（2026-08-09）：使用者只按一次「立即同步」；截圖中的最近嘗試／最近成功均更新為23:05:38、job=`READY`／attempt 0、最近錯誤無且按鈕已恢復，左下角為`0 待同步`且無取消錯誤。D1去敏唯讀核對確認新MANUAL run於23:05:19開始、23:05:38完成`SUCCEEDED`，fetched 4／updated 32／error 0；job、connection、兩種token密文、語意唯一性及財務隔離均通過。但以`sync_run_id`直接查raw只得到本次新插入的`playlist_items`與`videos`各1列，因既有`provider_raw_payloads`以`provider_key + payload_kind + sha256`全域去重，內容未變的`channels`與`analytics`沿用舊raw ID，沒有獨立run↔raw關聯。原始payload未遺失，卻無法由最新run完整列舉fetched 4，與「每次外部資料皆有可追溯原始證據」要求衝突，故不得標`VERIFIED`。預定新增`migrations/0010_provider_sync_run_payload_links.sql`與`provider_sync_run_payloads`關聯、修改`src/worker/api/provider-sync.ts`每次同步都寫4個連結、更新資料匯出／文件及D1固定答案測試；不修改已套用migration 0004／0009。外部阻擋暫時無，使用者不得重複同步。
- AT-YT-05 per-run raw追溯修補與staging部署（2026-08-09）：新增`0010_provider_sync_run_payload_links.sql`與`src/worker/api/provider-raw.ts`；每次run依payload order寫關聯，即使全域raw雜湊去重命中既有列也不省略。OAuth callback與一般同步共用相同準備流程，完整JSON匯出／還原加入關聯表。固定答案以兩個run取得完全相同payload，證明raw總數1、兩個run各有order 0關聯；匯出／刪除／還原後關聯ID不變。lint、雙typecheck、38/38 unit、21/21 Worker／D1、client build、12個全新schema 10隔離D1 Playwright、正式碼／Secret／skip掃描及diff check通過；需求release gate正確因`SOC-009`、`SETUP-003`仍`IN_PROGRESS`而拒絕，未冒充全面完成。staging升級前schema 9、run 9、raw 14、孤立raw 0、RUNNING run/job皆0，並建立2,227,836 bytes、SHA-256 `E7526D3B2F251370BA553DF31FFB7AC46DE13F07916719F6B7FE09760F5E9DA9`且Git忽略的remote SQL備份。0010套用後schema 10、raw 14／link 14／distinct linked raw 14、孤立link 0、重複order 0、索引1且migration list空。Wrangler deploy雖exit 1，但版本`9a72219e-7880-4ec4-b673-0cb99b64791d`存在且deployment status為100%；四個Secret只核對名稱／`secret_text`，未登入health仍Access 302。部署後連線密文、READY attempt 0與財務0均保留；舊23:05 run仍誠實顯示fetched 4／link 2，未臆測回填。外部阻擋為本人只按一次「立即同步」，再驗證新run恰有四筆有序per-run link。
- schema 10後真實MANUAL與pending UI診斷開工（2026-08-09）：使用者已在staging YouTube卡片只按一次「立即同步」；按下後數十秒、少於一分鐘內「最近嘗試／最近成功」更新為23:36:01，connection仍`CONNECTED`、錯誤無、job=`READY`／attempt 0，但執行期間按鈕沒有顯示文件既定的「同步中」回饋。這與本檔先前明列的固定成功答案「pending時顯示同步中且停用」衝突；後端最終成功不能取代即時UI驗收。`SOC-009`與`SETUP-003`維持`IN_PROGRESS`。預定先唯讀核對最新MANUAL run、四類有序`provider_sync_run_payloads`、snapshot provenance／唯一性、job與財務隔離，再比對staging bundle、`IntegrationsPage.tsx` mutation生命週期及service worker更新狀態；若確認UI缺陷，預定修改`src/app/pages/IntegrationsPage.tsx`與對應unit／Playwright驗收，必要時更新操作文件並重新部署。預期不需migration，不修改`0001`～`0010`；外部阻擋暫無，使用者不得重複同步、重新整理或撤銷連線。
- schema 10真實MANUAL後端結果與PWA根因修正（2026-08-10）：staging D1單一去敏唯讀聚合確認23:36:01最新MANUAL=`SUCCEEDED`、duration 19秒、fetched 4／created 0／updated 32／ignored 0／error 0；run link恰為4筆、order distinct 4／min 0／max 3、channels／playlist_items／videos／analytics各1、非法raw link 0、linked snapshot 545。connection=`CONNECTED` 1、job=`READY`／attempt 0，social account／post語意重複群組0，三個財務核心表仍0；APAC／HKG primary回報`rows_written=0`／`changes=0`。schema 10的真實per-run raw追溯已通過。部署中的`IntegrationsPage.tsx`其實已有`isPending`→「同步中」與同步／撤銷鎖定，畫面未顯示的根因是`public/sw.js`永久固定`life-manager-shell-v1`／`runtime-v1`且同名資產cache-first，既有裝置能無限期停留舊bundle。修正改為app shell內容SHA-256短版本、同源預快取、靜態資產network-first／離線fallback、`updateViaCache: "none"`與主動`registration.update()`，並補`pwa-build.test.mjs`、`pwa-update.test.tsx`、`integrations-page-pending.test.tsx`及Playwright pending案例。本輪不需migration、不修改`0001`～`0010`。lint、雙typecheck、unit 42/42、Worker／D1 21/21、client build版本`8ebf78fa53f6b382`、13個全新schema 10隔離D1 Playwright、正式碼／Secret／skip掃描與diff check均通過；requirements gate只因`SOC-009`、`OFF-001`、`SETUP-003`依法仍為`IN_PROGRESS`而拒絕。等待staging部署與本人安全更新驗收，三項不得提前標`VERIFIED`。
- PWA更新修正staging部署結果（2026-08-10）：從同一工作樹ASCII junction部署已驗證的`dist`，Wrangler上傳新版`/sw.js`與`/assets/app.js`並建立Worker版本13 `35796472-1e05-4931-b3f8-8f9aa6b6647c`；CLI在完成upload、trigger與輸出version後仍依已知Windows行為exit 1，未把非零改寫成成功。後續`deployments status`唯讀確認該版本承接100%流量，`versions view`確認assets、staging D1、fetch／scheduled handlers、60分鐘OAuth state、Access及四個必要`secret_text`名稱／型別完整，未讀取值；remote migration list為空，未登入health維持Access 302。部署不套migration、不寫D1、不改Secret。`SOC-009`、`OFF-001`、`SETUP-003`改為`AWAITING_USER_SETUP`；下一個唯一人工操作只是重新整理目前外部連線頁一次並回報是否出現「有新版可用」，不得同時按安全更新、同步或撤銷。
- PWA更新提示可見性續作開工（2026-08-10）：使用者完成唯一重新整理操作並確認「有新版可用」確實出現，但只有滾到頁面最底部才看得到。Codex以使用者已登入的Codex瀏覽器重現同一staging頁：初始`scrollY=0`、viewport高1075px、文件高1116px，`.update-banner`為`position: static`／`z-index: auto`，rect top=1075.39px、bottom=1115.38px，完全落在初始可視範圍外。程式原因是`PwaUpdate`渲染在`RouterProvider`之後而`src/styles.css`沒有`.update-banner`規則；這與`UI-006`重要資訊不可隱藏及`AT-OFF-08`更新提示可見要求衝突，不符合預期。`SOC-009`、`OFF-001`、`UI-006`、`SETUP-003`改為`IN_PROGRESS`。預定修改`src/styles.css`、`tests/e2e/app.spec.ts`及受影響的`docs/ACCEPTANCE_TESTS.md`、`docs/OPERATIONS.md`、`docs/SETUP_CHECKLIST.md`、`docs/TRACEABILITY_MATRIX.md`；必要時只補`PwaUpdate.tsx`可及性語意。預定執行targeted unit／Playwright、lint、雙typecheck、Worker／D1、client build、完整隔離D1 Playwright、正式碼／Secret／skip／需求狀態掃描與diff check；不需migration、不修改`0001`～`0010`。完整Worker／D1重跑另抓到OAuth state `created_at`與`expires_at`分別取時造成偶發1ms差異，與既定60分鐘精確固定答案衝突；預定最小修改`src/worker/api/oauth.ts`讓同一state共用一次伺服器時間並以原測試重驗，不放寬斷言。已登入瀏覽器使外部操作暫無阻擋；部署後先安全更新，再由Codex只按一次YouTube「立即同步」完成pending UI與去敏D1驗收。
- PWA提示與YouTube正式驗收結果（2026-08-10）：`src/styles.css`把更新提示固定於初始viewport，桌面右下且手機`bottom: 116px`避開同步狀態與導覽；`tests/e2e/app.spec.ts`以320／390／768／1366／1920五種viewport驗證四邊皆在可視範圍、`position=fixed`、層級大於20及手機不重疊。OAuth state另改為同一次`Date.now()`衍生建立與到期時間，保留精確3,600,000ms固定答案。lint、雙typecheck、unit 42/42、Worker／D1 21/21、client build版本`83d60343fa6e6772`、13個隔離D1 Playwright案例、正式碼／Secret／skip掃描與diff check通過；E2E期間一個Wrangler／Miniflare程序崩潰依既定規則以全新D1重試一次後通過，沒有重試產品斷言。版本14 `488a92a7-3ff1-47ec-8a04-49c8b75572a0`由同一worktree ASCII映射部署且CLI exit 0，獨立確認100%流量、assets、D1、fetch／scheduled、60分鐘OAuth與四個必要`secret_text`名稱／型別完整，remote migration list空，未登入health維持Access 302；未讀Secret值、未套migration、未寫D1。已登入Codex瀏覽器先確認舊提示為static且outbox 0，只按一次「安全更新」後自動reload；新正式CSS為`position: fixed`／`z-index: 30`，無清站台資料。接著只按一次YouTube「立即同步」，約1.6秒內按鈕變成停用「同步中」且撤銷停用，畫面約38.5秒內更新完成；D1實際run為17:02:25.100Z至17:02:43.660Z、19秒`SUCCEEDED`、fetched 4／created 0／updated 32／ignored 0／error 0、四筆raw link order 0～3且四類各1、linked snapshot 545、語意重複0、healthy connection 1、READY job 1、財務三表0，APAC／HKG primary回報`rows_written=0`／`changes=0`。最終112-ID一致性閘門通過（`VERIFIED=75`、`AWAITING_USER_SETUP=37`）。`SOC-009`、`OFF-001`、`UI-006`與`SETUP-003`改為`VERIFIED`，無外部阻擋。

## 2026-08-02 本輪開工紀錄

### 2026-08-03 03:03 最終本機部署閘門稽核（03:13完成）

- 稽核狀態：`VERIFIED`。範圍為`AT-GATE-01`至`AT-GATE-07`可在本機完成的部分；單一完整`npm run verify`於03:07開始，03:13以exit 0完成，總耗時359.8秒。
- 預定修改：若證據一致，只更新`docs/IMPLEMENTATION_STATUS.md`；若發現衝突，依實際缺口修改對應程式、測試及文件。既有`0001`~`0008`不得修改，預期不需新migration。
- 預定測試：需求112 ID雙帳本、lint、client／Worker typecheck、27 unit、16 Worker/D1與API契約、client build、12個隔離D1 Playwright、正式碼假資料／秘密／未實作與skip掃描，以及fresh migration。
- 外部阻擋：Cloudflare CLI尚未登入；`AT-GATE-08`、真實跨裝置、Firstrade CSV、YouTube、Instagram、Push、Resend及production Access不納入本機完成宣稱，維持`AWAITING_USER_SETUP`。
- 稽核衝突（03:05）：`scripts/scan-production-placeholders.mjs`的未實作規則只匹配`TO_DO/FIX_ME`，未實際匹配規格要求的`TODO/FIXME`；測試跳過規則也未涵蓋`.fixme()`。預定修改該掃描器並加入規則自我驗證；完成前`AT-GATE-03/04`不得視為本次已通過。
- 稽核衝突（03:07）：`scripts/verify-requirements.mjs`原先只驗證ID字串存在，歷史紀錄也可造成誤通過，沒有證明每個ID位於正式狀態表。預定改為解析狀態表、展開`PREFIX-001~NNN`、拒絕衝突或未收斂狀態，並要求`AWAITING_USER_SETUP`具體記載外部等待原因。
- 稽核結果：掃描器現已實際阻擋`TODO`、`FIXME`、`NotImplemented`及`.skip/.todo/.fixme/.only`，並以內建sentinel自驗規則；`src`、`public`及13個測試／設定檔通過。需求驗證器現解析兩種正式狀態表格式及範圍，證明112個第一批ID狀態一致：71個`VERIFIED`、41個具體外部閘門的`AWAITING_USER_SETUP`，無衝突、`IN_PROGRESS`或`IMPLEMENTED_UNVERIFIED`第一批ID。
- 安全收斂：`.tmp/`內的Playwright trace解包及`backups/`內的D1 SQL／SHA證據均保留在本機，但整個目錄加入`.gitignore`，避免未來含真實資料的診斷或備份檔誤入Git。
- 實際修改：`.gitignore`、`scripts/scan-production-placeholders.mjs`、`scripts/verify-requirements.mjs`、`docs/IMPLEMENTATION_STATUS.md`；不需migration，未修改`0001`~`0008`。

### 2026-08-03 01:24 圖表與完整viewport證據稽核

- 需求：`CORE-006`, `FIN-003`, `FIN-008`, `SOC-004`, `SOC-005`, `SOC-008`, `UI-001~006`, `UI-CHART-001~010`先改為`IN_PROGRESS`；既有API與計算證據保留，但不得用未覆蓋圖表語意的離線E2E支撐完整UI驗收。
- 規格／程式衝突：`ARCHITECTURE.md`指定Recharts加專案wrapper，目前`MetricLineChart.tsx`以自製SVG繪圖；`AT-CHART-01~10`要求tooltip、設定、事件hover／點擊與更新後曲線改變，目前Playwright未逐項檢查；`AT-UI-03`要求320、390、768、1366及大型桌面，目前只有320、390、1366。
- 預定修改：`src/components/charts/MetricLineChart.tsx`、`src/app/pages/SocialPage.tsx`、必要樣式、`playwright.config.ts`、`scripts/run-e2e.mjs`、`tests/e2e/app.spec.ts`及受影響文件；不修改既有migration，預期不需新migration。
- 預定測試：D1建立財務／指標／社群正式資料後驗證圖名、軸、刻度、單位、圖例、tooltip、定義、來源、篩選／聚合、事件標註與資料更新；另驗證768px與大型桌面、首頁順序及reduced-motion。
- 外部阻擋：Cloudflare仍未登入；圖表與viewport稽核完全可在本機完成，不以外部設定延後。
- 延伸衝突（01:35）：財務分析圖表的`lastUpdated`由前端呼叫`new Date()`產生，並非伺服器計算時間；`finance/analysis`與`net-worth-trend`也缺少圖表層級完整`AnalyticResult`。因此`CORE-007`、`FIN-004`、`FIN-006`同步改為`IN_PROGRESS`，預定修改`src/modules/finance/query.ts`、`FinancePage.tsx`與Worker契約測試；不需migration、無外部阻擋。

### 2026-08-03 02:58 圖表與完整viewport稽核結果

- `CORE-006`, `CORE-007`, `FIN-003`, `FIN-004`, `FIN-006`, `FIN-008`, `SOC-004`, `SOC-005`, `SOC-008`, `UI-001~006`, `UI-CHART-001~010`均完成缺口修正並改為`VERIFIED`。
- `MetricLineChart.tsx`改用規格指定的Recharts wrapper；財務、指標與社群圖表共用明確軸名、刻度、單位、圖例、鍵盤tooltip、事件標記與可展開provenance。社群新增五數分布、事件類型篩選及hover／focus／click狀態。
- 財務analysis與net-worth-trend由伺服器回傳完整`AnalyticResult`，不再用前端目前時間冒充計算時間；Worker固定答案驗證來源、樣本、觀測、缺失、排除、時間窗、篩選、分組、聚合、分母及`calculatedAt`。
- 首頁把今日行動放在第一個主區塊，超重要期限同時提供行內可操作入口與中斷式對話框；320、390、768、1366、1920px及reduced-motion均有Playwright證據與全頁截圖。
- 同步UI修正已完成上傳卻因重分析仍顯示「同步中」的競態；E2E改以`sync/batch`的`APPLIED`與後續`sync/changes`正式snapshot驗證D1寫入及下行同步，避免Windows本機Wrangler在離線恢復後被第二網路client並發讀取觸發崩潰。只有健康檢查確認Worker死亡才允許最多兩次全新D1重試，產品斷言失敗不重試。
- 實際修改：`src/components/charts/MetricLineChart.tsx`、`src/components/design-system/Panel.tsx`、`src/app/pages/FinancePage.tsx`、`MetricsPage.tsx`、`SocialPage.tsx`、`HomePage.tsx`、`src/app/providers/SyncProvider.tsx`、`src/modules/finance/query.ts`、`src/styles.css`、`playwright.config.ts`、`scripts/run-e2e.mjs`、`tests/e2e/app.spec.ts`、`tests/worker/api-d1.test.ts`及本輪受影響文件。
- migration：不需新增；未修改`0001`~`0008`。外部設定閘門未變。

### 續作完成性稽核（23:10開始）

- 需求：`CORE-002`, `CORE-003`, `TASK-003`, `OFF-003`先改為`IN_PROGRESS`；若稽核發現其他證據不足，必須同步降級，不沿用先前宣稱。
- 預定修改：`src/app/pages`、`src/app/hooks/use-resource.ts`、`src/core/sync`、對應module/API；若資料欄位不足只新增`0009_*.sql`，不得修改`0001`~`0008`。
- 預定測試：新增D1契約與Playwright，明確覆蓋延後任務、事業跨模組關聯，以及任務完成、財務交易、資產快照、指標、事件、社群快照／成交、期限完成的離線新增、編輯、刪除／封存、重開與恢復同步。
- 外部閘門：Cloudflare仍未登入；YouTube、Instagram、Firstrade真實CSV、Push、Resend及兩台實體裝置仍維持既有`AWAITING_USER_SETUP`，不影響本輪本機修正。

### 續作完成性稽核結果（2026-08-03 01:20）

- `CORE-002`：補齊通用事業跨模組關聯schema、API、七種正式目標驗證、UI建立／移除與D1契約，改為`VERIFIED`。
- `CORE-003`：補齊各自定義管理資料的編輯、封存／恢復或刪除操作路徑與唯讀provider邊界，改為`VERIFIED`。
- `TASK-003`：首頁可完成與版本化延後；補產生漏掉的歷史發生項、延後日期驗證、audit及離線同步，改為`VERIFIED`。
- `OFF-003`：修正RESTORE欄位、Service Worker離線資產、首次同步競速、共用請求閘門、同步中再次觸發補跑、每輪30秒逾時及非同步表單reset；27種核心輸入類型unit與完整UI重開生命週期通過，改為`VERIFIED`。
- migration：不需新增；現有`0001_core.sql`已有`entity_links`，`0002_tasks.sql`已有發生項延後欄位，`0006_sync.sql`已有裝置游標與change log；未修改任何已套用migration。

### 開工時現況差距

- 工作區只有規格文件；沒有Git repository、`package.json`、正式程式、D1 migration、測試或部署設定。
- `TRACEABILITY_MATRIX.md`提到`ARCH-001~008`，但`ARCHITECTURE.md`只正式定義`ARCH-001~003`。本輪以已定義的三項施工，未自行捏造`ARCH-004~008`；需後續由規格決策補正矩陣文字。
- 所有功能均無實作證據；所有外部整合也尚無帳號、secret、真實CSV、裝置或雲端Access驗收。

### 本輪完成結果

- 已建立正式React／Worker／D1模組化單體、八個只追加migration、本機D1、PWA與完整測試工具鏈；production不seed使用者資料。
- 所有不依賴外部帳號的第一批功能已依上方帳本逐項實作並通過固定答案、D1契約與五種viewport真實E2E。
- 社群比較實際支援平均、總和、中位數與五數分布，保存的比較定義會控制API與圖表；轉化率同時保存總和比與個別比率平均。
- 操作／通知紀錄預設保留365日、OAuth state保留30日；同步change log只在所有有效裝置游標確認後清除。原始匯入檔與provider原始證據不由此排程刪除。
- 仍未通過的項目只有帳本中標示`AWAITING_USER_SETUP`的真實外部驗收；沒有以mock、fixture或示範資料替代。

### 預定修改範圍

- 專案與工具鏈：`package.json`、TypeScript/Vite/Tailwind/Vitest/Playwright/Wrangler設定、CI與掃描腳本。
- Runtime：`src/worker`、`src/core`、`src/modules`、`src/integrations`、`src/service-worker`。
- UI：`src/app`、`src/components`、`public`。
- 資料：新增`migrations/0001_core.sql`至`0006_sync.sql`；首次本機套用後，依寫入冪等與系統級通知／provider定義需求另新增`0007_api_idempotency.sql`及`0008_operational_defaults.sql`，未修改已套用migration。`0008`只建立正式系統設定，不建立使用者或示範資料。
- 測試：`tests/unit`、`tests/database`、`tests/api`、`tests/integration`、`tests/e2e`與本機D1還原驗證。
- 文件：本檔、`TRACEABILITY_MATRIX.md`、`DATA_AND_SYNC.md`、`OPERATIONS.md`、`SETUP_CHECKLIST.md`與受影響架構說明。

### 外部設定閘門

- Cloudflare帳號登入、staging/production D1、Worker、Access政策與正式部署。
- Google OAuth client憑證Secret、真實YouTube頻道授權與同步證據。
- Meta App與真實Instagram專業帳號授權。
- 使用者提供不進Git的Firstrade遮蔽實際CSV。
- Resend帳號、API key、使用者本人收件信箱。
- 手機及電腦的Web Push權限與真實接收。
- 兩台真實裝置的PWA離線與跨裝置驗收。

## 延後需求

| 需求ID | 狀態 | 說明 |
|---|---|---|
| DEFER-001 | NOT_STARTED | 人際關係正式模組，等待使用者定義 |
| DEFER-002 | NOT_STARTED | 腦子面正式模組，等待使用者定義 |
| DEFER-003 | NOT_STARTED | Facebook、Threads、方格子API |
| DEFER-004 | NOT_STARTED | Firstrade正式自動provider |
| DEFER-005 | NOT_STARTED | 深度投資帳務與損益 |

延後需求不得在正式導航顯示空白頁或「即將推出」卡片。

## Release證據

### Release名稱
`R1-formal`

### 最近測試
- 日期：2026-08-03 11:08（Asia/Taipei）
- commit：本輪開工基準`21a96cb2f5dc46359261b9f0b05a57de56f79bd0`（`master`=`origin/master`，tag `local-verified-baseline`）；本輪staging設定與證據尚未commit。
- lint：`npm run lint`，通過，0 warnings。
- typecheck：`npm run typecheck`，client及Worker皆通過。
- client build：`npm run build:client`通過，798 modules；輸出`app.js` 958.97 kB（gzip 276.06 kB）。Vite仍提示單chunk超過500 kB，且`rrule`套件發出缺上游sourcemap來源警告；兩者不影響本輪正確性閘門，但production效能仍隨`NFR-001~010`維持雲端實測前的`AWAITING_USER_SETUP`，未宣稱已完成真實效能驗收。
- unit：`npm test`，9 files、27/27通過；含27種核心輸入類型離線生命週期與outbox 108筆不遺失、同步併發觸發補跑及30秒逾時固定答案。
- database：`npm run test:worker`，真實D1 migration/schema/CRUD/retention/tombstone/RESTORE共16/16通過。
- API contract：包含於Worker 16/16；Zod錯誤、冪等、版本衝突、OAuth callback、七種事業關聯、任務延後、同步RESTORE與分析固定答案通過。
- Playwright：`npm run test:e2e`，8個獨立desktop正式流程，加上768px、1920px、mobile-390、mobile-320，共12/12通過、exit 0、356.3秒；每組使用全新D1並套用`0001`~`0008`。覆蓋線上D1寫入、離線生命週期、同步協定snapshot、完整圖表語意與互動、D1更新曲線、首頁層級、reduced-motion及無水平溢位。
- 完整閘門：`npm run verify`於2026-08-03 09:48重新執行，依序完成lint、雙TypeScript、27 unit、16 Worker/D1與API契約、client build、12個隔離D1 Playwright、正式碼掃描及112-ID狀態表驗證，09:57完成，exit 0、總耗時510.9秒。
- fake-data scan：`npm run scan`，`src`及`public`通過；未發現fixture import、demoData、Math.random、秘密、任意程式執行、Firstrade帳密或RSC正式入口；掃描規則sentinel自驗通過。
- unfinished scan：通過；`TODO`、`FIXME`、`NotImplemented`為正式碼阻擋項；13個測試／設定檔未發現`.skip`、`.todo`、`.fixme`或`.only`。
- requirement status gate：完整本機閘門時為`VERIFIED=71`、`AWAITING_USER_SETUP=41`；staging Access與電腦／手機實體App證據完成後為`VERIFIED=72`、`AWAITING_USER_SETUP=40`；`OFF-005`兩台實體裝置與D1唯讀聚合驗收完成後為`VERIFIED=73`、`AWAITING_USER_SETUP=39`；本輪`INV-002`／`SETUP-007`真實Firstrade匯入及AT-INV-05人工核對完成，最新為`VERIFIED=77`、`AWAITING_USER_SETUP=35`。
- migration fresh：`applyD1Migrations`套用`0001`~`0008`，schema version 8、使用者資料表全空、僅正式系統預設；通過。
- migration upgrade：本輪本機D1按新增順序套用`0001`~`0008`，最後`wrangler d1 migrations apply --local`回報no migrations；未修改已套用migration。因尚無上一個release，無跨release資料集。
- staging migration：D1 `4a9da6fd-2fe5-41e1-b17f-0f444e60e14c`由0 tables依序套用`0001`~`0008`，schema version 8、五個核心使用者表0筆、remote migration list為no migrations；未修改既有migration。
- backup restore：`life-manager-local-20260803-002507.sql`，40,672 bytes，SHA-256 `3b97e7abadd7cd2478fd77c0a382f1629aee9e2110a2abe085acb8dd76e633fe`；隔離庫108 SQL commands、schema 8及核心表計數驗證通過。
- dependency audit：`npm audit --omit=dev`回報2個high，皆為同一React Router RSC advisory；本產品未使用RSC入口，掃描阻擋引入，殘餘風險記錄於`OPERATIONS.md`，未宣稱零漏洞。

### 真實外部驗收
- Cloudflare CLI：2026-08-03 09:45以Wrangler 4.118.0執行`npx wrangler whoami`，確認OAuth登入`bigtreepolo@gmail.com`、帳號ID `f9e3db25e1ef3e30994c705149f122ae`。
- Cloudflare staging：D1、Worker、migration、workers.dev URL、Access啟用前靜態資產200／API fail-closed，以及Access啟用後無session首頁/API 302完成；2026-08-09 D1已由8→9→10依序套用新增`0009`與`0010`，兩個social snapshot partial unique index及per-run raw link索引存在且migration list空。migration 0010前remote SQL備份已核對bytes與SHA-256；YouTube同步與per-run raw追溯修正部署為版本`9a72219e-7880-4ec4-b673-0cb99b64791d`且100%流量，四個必要Secret名稱／型別仍存在；本人登入health 200／staging／schema 8是升級前歷史證據，schema 10本人session健康檢查尚待更新。
- Cloudflare Access：使用者已將真實中文「網域」頁的生產Worker URL由「公開」改為「受限」；無session邊界拒絕、JWK 200及本人登入health 200通過，team domain／application audience與`ACCESS_ALLOWED_EMAIL`皆已設定，部署後Secret名稱仍存在且未讀取值；使用者確認只有本人單一Allow政策，電腦與手機實體App首頁均正常。Staging Access完成；Production Access仍等待SETUP-009。
- YouTube：Project ID `life-manager-personal-505006`下的Web OAuth client、三個必要Secret、兩項唯讀scope與60分鐘state已完成；第二次真實callback成功並建立AES-GCM access／refresh token密文。修正版真實Cron已成功，D1去敏聚合確認1頻道、31影片、574快照、raw provenance缺失0、同語意鍵重複0；本人重新整理後仍確認job=`READY`／attempt 0、connection=`CONNECTED`／錯誤0。Studio與API對齊2026/7/12至2026/8/6後觀看次數總計布林式精確相等，`AT-YT-04`通過；Google App已切換「實際運作中」且未提交公開驗證，等待AT-YT-05撤銷重連驗收。
- Instagram：adapter、scope/state測試完成；等待Meta App及真實專業帳號。
- Firstrade CSV：`INV-002`／`SETUP-007`已`VERIFIED`。正式staging batch完成486／486／0／0，App結果與D1一致；D1去識別Amount合計USD 17.81、活動分類及未知Other原始證據完整，重跑0新增／486重複；Firstrade官方完整日期範圍總數486，唯讀篩選確認2025-04-16兩筆MSTU BUY的數量與金額均一致；正式staging D1 SQL／App完整JSON備份已核對，未遮蔽資料不得進入本線。
- Web Push手機：密文、多裝置及停用契約完成；等待真實手機接收。
- Web Push電腦：密文、多裝置及停用契約完成；等待真實電腦接收。
- Resend Email：adapter、去重、retry完成；等待API key、from與本人收件。
- PWA離線：自動化五種viewport通過；等待production手機加入主畫面實測。
- 跨裝置同步：兩邏輯裝置與衝突自動測試通過；手機真實離線領域outbox `0→1→0`、不同實體電腦取得同一資料且`0 待同步`，D1聚合證明1筆已套用operation、0筆未套用、2台有效裝置與兩個游標皆到1，`OFF-005`已`VERIFIED`。

### 未完成清單
不得填「無」除非所有第一批項目為`VERIFIED`。

- `SOC-010`, `DDL-008`, `DDL-009`仍等待各自真實資料／帳號／裝置；`SOC-009`的真實YouTube授權、撤銷、重連、同步與來源證據已`VERIFIED`。`INV-002`／`SETUP-007`已在本輪真實Firstrade匯入及AT-INV-05核對完成後標為`VERIFIED`。
- `SEC-001`, `SEC-002`, `SEC-003`, `SEC-004`, `SEC-005`：本機安全邊界完成，等待真實Cloudflare Access與外部通道驗收。
- `NFR-001`~`NFR-010`, `OPS-001`~`OPS-011`, `SETUP-001`, `SETUP-004`~`SETUP-006`, `SETUP-008`~`SETUP-009`：本機可驗證部分與staging已完成；整組因production部署、外部服務及其真實裝置smoke尚未完成而維持`AWAITING_USER_SETUP`。`SETUP-002`、`SETUP-003`與本輪`SETUP-007`已各自獨立`VERIFIED`。
- 唯一下一步：Codex實作並驗證migration 10的provider run↔raw payload完整關聯；使用者保持目前頁面，不重複同步、重新整理或撤銷連線。
